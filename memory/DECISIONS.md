# DECISIONS.md — Принятые решения

<!-- Заполняет архитектор. Каждое решение: ID, суть, rationale, отклонённые альтернативы. -->

## Решения

| ID | Решение | Rationale | Альтернативы (отклонены) |
|----|---------|-----------|--------------------------|
| D-001 | HTML+Tailwind CDN как default стек для Make UI | Простота, нулевой setup, один файл, работает везде | React+Vite (overhead), Next.js (нужен сервер) |
| D-002 | Tokens отдельно, темы отдельно | Переиспользование базы правил между темами | Всё в одном файле (дублирование между темами) |
| D-003 | Inter как default шрифт | Стандарт индустрии 2024-2025, поддерживает tracking-tight | System fonts (быстрее, но выглядят устаревшими) |
| D-004 | Border вместо shadow для карточек | Современный тренд, легче визуально | Box-shadow на карточках (устаревший паттерн) |
| D-005 | Coder модель: kp/deepseek-v4-pro | kp/glm-5.2 недоступна (503), deepseek-v4-pro мощнее (reasoning, 128K) | GLM-5.2 (недоступна на момент теста) |
| D-006 | Make-ui — ОБЯЗАТЕЛЬНЫЙ промпт для всех UI | Архитектор использовал свой промпт для портфолио → результат «фигня». Теперь make-ui.md — единственный разрешённый промпт для UI | Свободный промпт архитектора (неконтролируемое качество) |
| D-007 | Verify loop (screenshot→designer→fix) — ОБЯЗАТЕЛЕН | Без проверки дизайнером layout ломается (наезжающие элементы) | «Готово» без визуальной проверки |
| D-008 | ❌ **ОТМЕНЕНО (D-023)** — ~~Миграция clipproxy → DashScope (прямой доступ)~~ | ~~clipproxy недоступен, DashScope даёт прямой доступ~~ | — |
| D-009 | ❌ **ОТМЕНЕНО (D-023)** — ~~DashScope как основной провайдер~~ | ~~API совместим с OpenAI, vision-модели доступны~~ | — |
| D-010 | qwen-vl-max для vision-агентов | Самая мощная vision-модель на DashScope, поддерживает изображения | qwen3-vl-plus (менее мощная), clipproxy/vl (недоступен) |
| D-011 | NVIDIA как бэкап-провайдер | DeepSeek V4 Pro доступен через NVIDIA, на случай проблем с DashScope | Основной провайдер — DashScope |
| D-012 | Использовать deepseek-v4-pro как основную модель для кодинга и размышлений | Модель работает через DashScope, мощнее glm-5.2, поддерживает 128K context, хорошая reasoning | kp/glm-5.2 (недоступна), kp/deepseek-r1 (менее мощная) |
| D-013 | ❌ **УСТАРЕЛО (D-023)** — ~~clipproxy как бэкап для dashscope~~ (теперь clipproxy = primary, DashScope = fallback) | ~~clipproxy недоступен~~ | — |
| D-014 | Добавлены новые темы: luxury (Apple), gaming (Neon/Retro), dark-tech (Cyberpunk) | Увеличение гибкости системы, поддержка различных стилей UI | Не добавлять новые темы (ограничение выбора) |
| D-015 | Создан план развития системы: направления сайтов, контент-блоки, генерация изображений | Улучшение качества контента и автоматизации процесса | Продолжать без плана (риски несогласованности) |
| D-016 | Добавлены направления сайтов: медицинский, строительный, трейдинг, лайфстайл | Увеличение точности определения стиля и подбора контента | Не использовать направления (ограничение выбора) |
| D-017 | Автоматически запускать make-ui при любом запросе о дизайне (редизайн, изменение блока, перемещение элемента) | Обеспечение качества UI через стандартный процесс | Использовать свободные промпты (неконтролируемое качество) |
| D-018 | Создан skill `ui-request-handler` для автоматического запуска make-ui | Упрощение работы с дизайном, гарантия качества | Не использовать skills (сложность) |
| D-019 | Обновлён skill `ui-request-handler` с полным списком ключевых слов | Гарантия запуска make-ui при любом упоминании дизайна | Использовать частичный список (риски пропуска) |
| D-020 | Архитектор переведён с vision (qwen-vl-max) на reasoning (deepseek-v4-pro) | Архитектор планирует и делегирует, не анализирует изображения | Vision на архитекторе (тратит ресурсы на то, что не нужно) |
| D-021 | Создан ui-coder агент для HTML/Tailwind/UI (вместо coder) | coder был Python-разработчиком, не знал фронтенд | Использовать coder для UI (генерировал Python вместо HTML) |
| D-022 | make-ui.md обновлён: все ссылки coder → ui-coder | Гарантия что архитектор делегирует UI правильному агенту | Оставить coder (не умеет UI) |
| D-023 | clipproxy восстановлен как primary провайдер, DashScope как fallback | clipproxy работает и предоставляет 41 модель, DashScope как backup | Только DashScope (ограничивает доступ к моделям) |
| D-024 | Контент UI всегда на языке владельца (default: русский) | Владелец русскоязычный, весь текст на сайте должен быть на русском | Английский контент (владелец не понимает) |
| D-025 | Генерация картинок через DashScope Wanx (агент image-gen) | placehold.co — заглушки, нужны реальные изображения. wanx2.1-t2i-turbo = 6 сек/картинка | Только placehold.co (выглядит дёшево) |
| D-026 | Скрипт gen_image.py: D:/pi/scripts/gen_image.py | Единая точка генерации: submit → poll → download. Поддержка batch | Каждый раз писать curl вручную |
| D-027 | 3 вау-темы: bento (Apple), mesh-gradient (Stripe), aurora (Vercel) | Стандартные темы слишком однотипные. Bento = мозаика, mesh = цветные пятна + glass, aurora = dark + сияние | Только standard темы |
| D-028 | Wow-patterns.md: библиотека из 10 CSS-приёмов | Stagger reveal, spotlight, shimmer, marquee, typewriter и т.д. Coder подключает к любой теме | Каждый раз придумывать паттерны заново |
| D-029 | Вау-уровни: 1=standard, 2=enhanced, 3=wow | Архитектор определяет уровень и подключает wow-patterns.md при уровне 2+ | Все сайты одинаково "вау" или одинаково скучные |
| D-030 | 3-осная модель вместо "режимов" | Input (что дали) × Fidelity (точность) × Scope (объём). 8 сценариев = комбинации 3 осей. Не 5 "режимов" | Жёсткие "режимы" не покрывают все случаи (франкенштейн, бренд, точечная правка) |
| D-031 | Style Guide = артефакт ДО генерации страниц | Из Relume: Style Guide генерируется первым, сохраняется, применяется ко всем страницам. Решает проблему консистентности на корню | Каждая страница генерируется отдельно → стили плывут |
| D-032 | 8 style categories (из Webflow) | Colors, Typography, Buttons, Spacing, Borders, Shadows, Images, Animations — полная структура кастомизации | Хаотичные CSS-правила без структуры |
| D-033 | style-guide.md: примеры реальных сайтов | 8 категорий: SaaS (Linear/Vercel/Stripe), Medical (Zocdoc), Portfolio (Brittany Chiang), E-commerce (Allbirds), Construction, Education (Duolingo/Coursera), Crypto (Coinbase), Food (Sweetgreen) | UI-coder генерирует вслепую без эталонов |
| D-034 | Конверсационный UX: архитектор спрашивает если неясно | Если input/fidelity/scope не ясны из описания → 1-2 вопроса. Не молча генерирует | Полная прозрачность vs полная невидимость — выбран баланс |
| D-035 | Project Model (project.json) как ядро системы | Без памяти проекта нельзя сказать "добавь страницу" без пересборки. Хранит: бренд, решения, структуру, todo, rejected | Каждый запрос = начинай с нуля |
| D-036 | Design Tokens в JSON (design-tokens.json) + markdown (style.generated.md) | JSON для programmatic use (HTML/CSS/React), markdown для людей | Парсить markdown программно = боль |
| D-037 | Component Registry (components.json + shared/components/) | Страницы = композиция компонентов (не HTML с нуля). Изменение nav.html = изменение на всех страницах | Каждая страница копирует nav/footer заново |
| D-038 | pages.json — карта сайта | Явная структура: каждая страница + её секции + компоненты | Нет явной структуры, генерируется на лету |
| D-039 | Style Guide: 12 категорий (расширено с 8) | Добавил Icons, Forms, Tables, Navigation — критично для больших сайтов | 8 категорий хватит для лендинга, но не для сайта клиники |
| D-040 | Отклонено: 25 категорий, Architect→4 агента, Reference Analyzer, Intent Parser, Asset Pipeline | Over-engineering для 1-пользовательской системы. 7 hops = 2-3 мин latency. image-gen покрывает 80% | Если architect.md > 3000 строк — пересмотреть |
| D-041 | Single Source of Truth (P1) | Каждая сущность хранится только в одном месте. design-tokens.json = цвета, components.json = компоненты, pages.json = страницы, project.json = состояние | Дублирование = рассинхронизация |
| D-042 | Design Tokens обязательны (P3) | UI Agent никогда не придумывает цвета/radius/spacing/shadows. Если токена нет — остановиться и запросить | Hardcoded цвета = нарушение архитектуры |
| D-043 | Incremental First (P4) | Приоритет: Component → Section → Page → Project. Не пересобирать страницу если можно изменить компонент | Полная пересборка при каждой правке = медленно |
| D-044 | Canonical Components (P5) | Правка сначала в shared/components/, потом все страницы. Запрет: править компонент только в одной странице | Проект "расползается" — разные версии nav на разных страницах |
| D-045 | Fallback Policy (P6) | Цепочка: project.json → design-tokens → style-guide → спросить пользователя. Не наоборот | Агент сразу спрашивает вместо поиска в артефактах |
| D-046 | Agent Contracts (Input/Output) | Каждый агент имеет чёткие Input и Output. Архитектор: query → project.json + task. UI-coder: tokens + components → HTML | Агент "забывает" что-то сделать без контракта |
| D-047 | Change Protocol (Analyze→Plan→Execute) | Перед изменением: analyze (что затронуто), plan (что пересобрать), execute (минимальный набор). Встроен в ui-coder, не отдельный агент | Случайные изменения без анализа зависимостей |
| D-048 | Build State встроен в project.json.status | Статусы: todo, building, done, failed, review. Позволяет продолжить после ошибки | Отдельный build-state.json = дублирование (нарушение P1) |
| D-049 | Component Version + used_by | components.json содержит version и used_by[] для каждого компонента. used_by = dependency graph | Нет версионирования, нет понимания зависимостей |
| D-050 | Усиленный Reference Pipeline (Style Extraction) | Извлекать не только цвета, но и: Grid, Spacing, Typography, Radius, Density, Mood, Shadows, Borders, Animation | Только цвета = неполная копия стиля |
| D-051 | Definition of Done расширен (14 пунктов) | +Консистентность токенов, +Нет вне Registry, +Нет hardcoded, +Нет дублирования, +Consistency Checker, +project.json ↔ pages.json | "Designer 7/10" недостаточно — нужны формальные критерии |
| D-052 | Отклонено: build-state.json, Version History | build-state объединён с project.json.status (P1). Version History отложена до итеративной работы | Over-engineering для текущей стадии |
| D-053 | Дизайнер галлюцинирует — всегда проверять grep | Дизайнер (8.8/10, 8.5/10) находил проблемы, которых нет в коде (rounded-full, shadow-lg, max-w-6xl). Ui-coder grep подтвердил: код чистый | Слепо доверять дизайнер-ревью без верификации |
| D-054 | Multi-page pipeline работает end-to-end | ЛапЛап: 4 страницы, 9 компонентов, Change Protocol, Consistency Checker — всё по P1-P7 | Single-page только (старый подход) |
| D-055 | Free fidelity + вау-темы работают | CyberSpace: bold-tech, 4 вау-паттерна (stagger, counter, shimmer, gradient border) | Только guided fidelity |
| D-056 | Nunito + Inter — валидная сочетание для тёплых тем | ЛапЛап: Nunito (headings) + Inter (body) = дружелюбный вид. Не запрещено токенами | Только Inter везде |
| D-057 | Subagent fallback mechanism | Добавлен fallbackModel в AgentConfig + automatic retry в subagent extension. Если primary модель fails (503, auth error, timeout) → retry с fallbackModel | Агенты падают при недоступности модели |
| D-058 | Vision-агенты: primary clipproxy/vl, fallback dashscope | clipproxy/vl/qwen3-vl-plus → dashscope/qwen-vl-max. Если оба недоступны — graceful degradation | dashscope primary (clipproxy VL не работает на 2026-07-15) |
| D-059 | Pre-flight health check + vision-блокировка | Subagent extension проверяет доступность модели перед запуском (ping max_tokens=1, cache 60s). Если primary + fallback недоступны → агент НЕ запускается с понятной ошибкой. Архитектор НЕ обходит блокировку, НЕ выполняет vision-работу вручную | Silent failure, галлюцинации от text-модели на vision-задачах |
| D-060 | SitAndEat.ru pixel-perfect — заблокирован до vision | Reference Copy требует vision для Style Extraction. Health check автоматически разблокирует когда VL-модели заработают | Обход через Playwright DOM extraction (нет цветов/стилей) |
| D-061 | SitAndEat разблокирован — vision восстановлен | clipproxy/vl/qwen3-vl-plus снова отвечает. Pipeline: Designer (Style Extraction) + Researcher (DOM) + Coder (генерация) | — |
| D-062 | fallback → fallbackModel во всех 16 агентах | Subagent extension парсит только `fallbackModel`. 12 агентов имели `fallback:` (не читался extension'ом) — массовый фикс | Агенты без fallback при недоступности primary |
| D-063 | SitAndEat 6.1/10 (v4) — приемлемо для reference copy | Все 7 секций в коде. Дизайнер галлюцинирует (D-053): не видит sticky nav, dropdown, advantages. Реальная оценка ~6.5/10 | Доводить до 10/10 (ROI низкий для pixel-perfect) |
| D-064 | Pixel-Perfect Pipeline: 5 фаз (CSS→Content→Style→Gen→DOM-diff) | Vision даёт approximations (#F97316 вместо #FF6B35). Решение: CSS extraction через getComputedStyle + Content extraction (verbatim DOM) + Vision (дополнение). Скрипт: `~/.pi/agent/scripts/extract-reference.js` | Vision-only extraction (неточные размеры/цвета) |
| D-065 | ZERO INVENTION rule для pixel-perfect | Ко́дер ЗАПРЕЩЕНО придумывать текст/картинки/размеры — только из extraction-файлов. Если данных нет → пропустить, не выдумывать | «Придумай реалистичный контент» для reference copy |
| D-066 | Pixel-Perfect Audit Protocol для дизайнера | Element-by-element сравнение: top bars → nav → hero → секции → footer → глобальные. Каждая претензия проверяется grep'ом (D-053) | Общие оценки («навигация похожая», 7/10) |
| D-067 | extract-reference.js — системный скрипт | `~/.pi/agent/scripts/extract-reference.js`: Playwright getComputedStyle + DOM traversal. Выводит css-extraction.md + content-extraction.md | Одноразовые extract-dom.js для каждого проекта |
| D-068 | Tailwind CDN ОБЯЗАТЕЛЕН в генерации | ui-coder НЕ добавил Tailwind CDN в SitAndEat v1 — сайт не отрендерился. Правило: `\<script src="https://cdn.tailwindcss.com">\<\/script>` в КАЖДОМ HTML | Положиться на ui-coder что добавит |
| D-069 | D-053 массово подтверждён в SitAndEat v1 | Дизайнер утверждал #F97316/Inter/rounded-full/О-Р — grep показал 0 вхождений. ВСЕ ключевые претензии — ложь. Реальная оценка ~8/10 vs заявленных 6.8/10 | Доверять designer-ревью без grep-верификации |
| D-070 | extract-reference.js: 3 бага extraction | **1)** Не захватывал `<input>`, `<form>`, `<select>`, `<textarea>` — поиск sitandeat.ru пропущен. **2)** Не извлекал grid-структуру header — логотип наезжал. **3)** Карточки без `info` text и overlay-ссылок — каталог без «145 позиций». Все 3 исправлены. | Полагаться на v1 extraction |
| D-071 | Отрицательные margins — ТОЧНЫЕ из CSS, не придумывать | `.header__logo` margin-top: -10px, `.main-nav` margin-left: -50px — реальные CSS значения. Но в копии создают наложение если grid не совпадает с оригиналом. Правило: отрицательные margins ТОЛЬКО если grid-структура 100% совпадает | Убирать все отрицательные margins в копиях |
| D-072 | extract-reference.js: карточки без CSS детей | **Корень бага «меню по-другому»:** скрипт извлекал только `textContent` карточек (title/info/price), но НЕ их CSS. UI-coder не видел `font-size: 22px` и `color: #FF7A00` → подставлял дефолт `16px #717171`. Добавлено: `titleCSS`, `infoCSS`, `priceCSS`, `overlayCSS`, `container padding/position` для каждой карточки. Output вырос с 31.7→37.3 KB | Полагаться на текст без CSS |
| D-073 | Правило для ui-coder: CSS детей ОБЯЗАТЕЛЬНО | Если в extraction есть `Title CSS: 22px`, `Info CSS: 14px, #FF7A00` — кодер ОБЯЗАН использовать ТОЧНЫЕ значения. Запрещено подставлять дефолтные body font-size/color для вложенных элементов карточек | Игнорировать CSS детей |
| D-074 | extract-reference.js + DOM-структура карточек | **Корень «всё крупнее» и «меню не то»:** (1) Скрипт извлекал только ТЕКСТ секций/заголовков/параграфов без CSS — добавлено sectionCSS/headingCSS/paragraphCSS + borderRadius/overflow. (2) **DOM-иерархия карточек:** оригинал использует wrapper-дивы (`.categories-top__img` absolute + `<img>` static, `.categories-top__btn` wrapper с margin 60px), кодер генерировал плоскую структуру. Grid: gap 24→30px, columns 440→446.656px. Добавлен Playwright CSS-diff скрипт для автоматического сравнения | Плоская структура вместо wrapper-дивов |
| D-075 | Background-color карточек + wrapper-уровни | **Корень «не выглядит как оригинал»:** (1) Карточки имели transparent bg вместо `#f1f1f1` (серый фон оригинала). (2) Внешний wrapper имел max-width 1400px вместо 1780px, отсутствовал padding 0 20px. (3) Не было промежуточного div (categories-top, display:block). Добавлено: `bg:` в Container extraction, правила ui-coder об обязательных wrapper-уровнях и background-color | Пропускать bg карточек и внешние wrapper-уровни |
| D-076 | Каталог: grid-template-rows обязательны | **Корень «каталог выше на 50%»:** ui-coder генерировал grid без `grid-template-rows`, из-за чего карточки растягивались по содержимому (274-298px вместо 180px). Оригинал использует `grid-template-rows: repeat(5, 180px)` для фиксированной высоты. Plus title/info позиционировались ПОД картинкой (flow) вместо НАД ней (z-index: 1 поверх absolute img wrapper). Карточка = `position: relative; height: 180px; padding: 20px; bg: #f1f1f1; border-radius: 15px; overflow: hidden` | Пропускать grid-template-rows |
| D-077 | Grid extraction + font/color на детях | extract-reference.js не захватывал grid-template-rows/cols для секцийных grid-контейнеров (только header). ui-coder не видел rows и генерировал auto-height. Plus Tailwind дефолты перебивали inline font-size/line-height/color на grid-контейнерах и детях (16px/24px вместо 18px/25.2px). Fix: extract-reference.js теперь захватывает grids[] (display, cols, rows, gap, firstChildCSS); ui-coder ОБЯЗАН явно указывать font-size/line-height/color на grid И каждом child | extract-reference.js пропускал секционные grids; ui-coder полагался на Tailwind дефолты |
| D-078 | gen_image.py: hardcoded API ключ убран (безопасность) | **Аудит H-1:** ключ DashScope `sk-4deb...` захардкожен как fallback → утечка секрета в VCS. Fix: убран fallback, добавлен `sys.exit(1)` если `DASHSCOPE_API_KEY` не задан. **Владельцу:** отозвать старый ключ в DashScope, сгенерить новый | Оставить ключ в коде (утечка секрета) |
| D-079 | mathematician: deepseek-r1 → deepseek-v4-pro | **Аудит H-2:** `clipproxy/kp/deepseek-r1` не отвечал, агент падал на fallback. Переведён на `clipproxy/kp/deepseek-v4-pro` (fallback `dashscope/deepseek-v4-pro`) — как у других reasoning-агентов | Оставить R1 (неработающая модель) |
| D-080 | Vision-агенты: primary clipproxy/vl → dashscope/qwen-vl-max | **Аудит H-3:** у VL-провайдера clipproxy НЕТ ключей (T-057), фактически работали через fallback. Frontmatter designer/screenshot/image-reader приведён к реальности: `model: dashscope/qwen-vl-max`, `fallbackModel: clipproxy/vl/qwen3-vl-plus`. D-058 и AGENTS.md summaries согласованы | Раздобыть ключи VL clipproxy (неизвестно когда) |
| D-081 | tokens.md: theme exceptions + spacing scale + Images/Icons/Forms/Navigation | **Аудит L-8/L-9/L-10:** (1) Темы gaming/dark-tech/bold-tech/bento/aurora/mesh-gradient нарушали anti-patterns (gradient/heavy shadows/rounded-3xl) — добавлена секция «Theme-specific exceptions» с таблицей. (2) warm-minimal использовал py-28/py-40 вне шкалы — spacing scale расширен. (3) Не было токенов для Images/Icons/Forms/Navigation — добавлены 4 секции | Менять темы под tokens (потеря эстетики) |
| D-082 | consistency-checker агент создан | **Аудит M-2:** агент был в DoD и Agent Contracts но файла не было. Создан `agents/consistency-checker.md` (VL-модель, grep+скриншоты, D-053 anti-hallucination). DoD теперь выполним | Убрать из DoD (потеря проверки консистентности) |
| D-098 | **КОРЕНЬ "я Notion AI" НАЙДЕН — clipproxy подменяет tools.** Все 15 coding/reasoning агентов переведены с `clipproxy/kp/*` → `dashscope/*` primary. Vision-агенты (designer/screenshot/image-reader/consistency-checker) уже на dashscope — не тронуты | clipproxy (CLIPPROXY_HOST) **ПОДМЕНЯЕТ function calling tools на Notion tools** — модель literally отвечает `"I can only use the Notion tools available"` когда pi передаёт write/bash. Это было корнём 5+ отказов coder/ui-coder/wp-coder ("я Notion AI без инструментов"). dashscope/deepseek-v4-pro + dashscope/qwen3.7-max стабильно передают tools (5/5 тестов ✅ tool_call write). Тест: coder создал файл после фикса (до — 2 отказа). **D-023 частично отменён:** clipproxy остаётся только для vision fallback (clipproxy/vl/qwen3-vl-plus) и main pi процесса (где tools inject'ятся иначе — architect работает на clipproxy в main). Для spawning субагентов clipproxy НЕ подходит | Починить clipproxy (нет доступа к конфигу прокси); nvidia (404 на /chat/completions); workaround через bash только (write/edit всё равно не работают на clipproxy) |
| D-099 | **КОРЕНЬ "no output" (subagent spawn failure) НАЙДЕН — `spawn("pi", {shell:false})` ENOENT на Windows.** `getPiInvocation` возвращает bare `"pi"` когда pi запущен под bun (виртуальный скрипт `/$bunfs/root/`), но `spawn("pi", {shell:false})` падает ENOENT (Windows требует `pi.cmd` или `shell:true`). Silent spawn error → empty messages + empty stderr → "(no output)". **Fix:** `getPiInvocation` теперь находит pi `dist/cli.js` на диске (Strategy 1: require.resolve; Strategy 2: derive из `process.execPath` — bun.exe в `<npm>/node_modules/bun/bin/` → `../../@earendil-works/pi-coding-agent/dist/cli.js` ✅) и запускает `process.execPath + cli.js` (работает с `shell:false`, подтверждено "BUN OK"). Fallback: `pi.cmd` с `shell:true` (detect `.cmd`). Оба spawn (primary + fallback) используют `shell: invocation.command.endsWith(".cmd")` | `shell:true` всегда (риски с args содержащими кавычки/метасимволы); хардкод пути (не портативно) |
| D-099b | **КОРЕНЬ "no output" повторно — несуществующий cwd → spawn ENOENT (Windows masquerade).** После D-099 fixed spawn всё равно 0/3. Debug-лог показал: `spawn C:\Program Files\nodejs\node.exe ENOENT` при существующем node.exe. **Классическая Windows-ловушка:** `spawn` с несуществующим `cwd` кидает ENOENT, ошибочно называя executable (а реально не найден cwd). Архитектор удалил `D:/pi/projects/_system_test` в cleanup → 3 параллельных теста + retry single все упали. **Fix:** extension вычисляет `effectiveCwd` — если cwd существует, использовать; иначе `mkdirSync(cwd, {recursive:true})`; иначе fallback на `defaultCwd`. Применён к обоим spawn (primary + fallback). Тест после создания cwd: **3/3 succeeded** (coder `Sum=5050`, ui-coder `grep=1 wc=13`, researcher `wc=7 grep=2`) | Удалить guard (recurrence при любом cleanup); игнорировать cwd (агент не сможет писать в нужную папку) |
| D-110 | **5 тем для СтройМакс готовы (D-104/108 реализованы).** graphite-mono (#44403C, Sora+Inter), sage-stone (#4A5D4F sage, Sora+Inter), dusty-slate (#475569 slate, Space Grotesk+Inter, light+dark), concrete-steel (#57534E stone, Space Grotesk+Inter, индустриальный, light+dark), editorial-cream (#6B5D4F mocha, Playfair+Source Serif). Все: CSS :root, --shadow-brand ЦВЕТНОЙ, cubic-bezier(0.4,0,0.2,1), full components, NO warm accents. showcase.html для выбора | Одну рекомендацию (владелец хочет несколько для сравнения) |
| D-111 | **IADS реализован как image-analyzer.js (D-103/105).** 889 строк, zero-dependency Node.js, dashscope qwen-vl-max vision, 40-rule decision matrix, 10 content_type, 8 handling strategies, gen_image.py integration. DASHSCOPE_API_KEY из env (D-078 соблюдено). Тест 5/6 ✅ | Внешние npm deps (sharp/image-size); хардкод ключа |
| D-112 | **Image analysis СтройМакс: ВСЕ фото низкого разрешения.** 6 featured images: 236-387px (score 5-6/10). 2 (Domino 342px, KAMENNYJ-CZVETOK 236px) требуют регенерации (gen_image similar 1024²). crop_safe: texture-macro=false (edge details), product-group=true (centered). **Конфликт:** IADS рекомендует cover для catalog-card, но владелец требует "в карточках не обрезались" → product.html должен использовать contain для карточек, IADS crop_safe показывает какие МОЖНО crop при необходимости. **Решение владельца (2026-07-22):** тема #4 Concrete Steel выбрана. Не все фото бесшовные → contain для карточек приоритет | Слепо следовать IADS cover (нарушит требование владельца); игнорировать IADS (потеря intelligence) |
| D-113 | **Skills Upgrade выполнен (ТЗ TASK-skills-upgrade.md).** Точечный перенос концептов из Anthropic frontend-design + Superpowers БЕЗ установки пакетов (D-100/D-101). 9 артефактов: few-shot.md (6 пар ❌/✅, наши темы, 0 выдуманных hex), Anti-Slop Design Principles (3 AI-клише+hero-антипаттерн+Signature Element+Pre-build Self-Critique), Chanel's Rule, UX-Writing Principles, Deep Critique Protocol (4 пункта на проблему+D-053 reminder), Progress Ledger Protocol (states todo→generated→review→fixed→approved, защита от compaction), File Handoff (task-brief в файл, экономия ~80% контекста), Two-Stage Review (Spec Compliance+Code Quality), Model Selection matrix, Rationalization Tables, a11y-check.js (WCAG AA: contrast/landmarks/alt/keyboard/aria, exit 0/1), WCAG AA contrast в tokens.md. Оценка 7.5→8.5/10. Стратегия: разделение по файлам (не этапам) → 7 параллельных subagent, нулевые конфликты | Установка Superpowers целиком (D-100); вливание frontend-design целиком (D-101); sequential по спринтам (медленнее) |
| D-114 | **a11y-check.js: WCAG AA аудит через Playwright.** 563 строки, zero-dependency (Playwright+built-in, axe-core опционален try/require). 5 категорий: contrast (relative luminance+ratio, 4.5:1/3:1), landmarks (header/nav/main/footer/aside), alt-тексты, keyboard-focusable, ARIA. getEffectiveBackground обходит прозрачных родителей. Smoke-test: 42 violations (1 critical+36 serious+5 moderate) на theme-showcase.html, exit 1 ✅. Интегрирован в verify loop make-ui.md | Внешние npm deps; хардкод путей |
| D-115 | **7-stage reboot выполнен (Concrete Steel, D-101).** Корень «провинциально» (D-100: Pi Make пишет HTML с нуля vs v0/Lovable shadcn/ui компоненты) → HTML Component Library + CSS Custom Properties + Font Pairing + Emotional Prompt + Hero Element + Photo System. (1) Component Library: 5 компонентов (nav/product-card/product-detail/features/footer, 872 строк), Concrete Steel :root, var(--color) через Tailwind arbitrary, cubic-bezier, colored --shadow-brand, object-contain, 0 hardcoded hex. (2) Системные правила: tokens.md (CSS Custom Properties D-107+Font Pairing D-108+IADS D-112), make-ui.md (CSS Custom Properties правило+Hero Element Rule+Image Analysis), ui-coder.md (Emotional Prompt «Make it BEAUTIFUL»+Signature Element техники). (3) product.html v6: 777 строк, FULL REBOOT с нуля (D-099*), Domino, ZERO INVENTION, Signature element display 72px, layered depth 4+, UX-writing, a11y landmarks. a11y: 16 SERIOUS на --color-text-muted (тема-inherent, основной текст AA) | Копирование старого layout (D-099*); хардкод hex; amber/terracotta (отвергнуты) |
| D-116 | **a11y тема-inherent: --color-text-muted #A8A29E не проходит AA.** Concrete Steel (и вероятно др. новые темы) имеют muted-цвет #A8A29E (stone-400) → 16 SERIOUS contrast violations на светлом фоне (#F5F5F4). Основной текст (#1C1917, #57534E) проходит AA. Eyebrow/breadcrumb/copyright = muted. Fix (pending): затемнить --color-text-muted до #57534E или принять как decorative (WCAG exempt). Решение за владельцем — не блокер для product.html v6 | Затемнить сейчас без решения (тема «вылизана»); игнорировать a11y полностью |
| D-117 | **КОРЕНЬ «какашечный везде» — тупое правило автовыбора тем.** AGENTS.md строка 281 «блог, контент, портфолио, lifestyle, docs → warm-minimal» направляло ВСЕ контентные сайты в warm-minimal (amber #d97706). Из 15 тем «светлая тёплая контентная» = только warm-minimal → альтернатив в автовыборе нет. warm-minimal = stone(cream)+Playfair(serif)+amber→terracotta = ТОЧНО наше AI-slop клише №1 (Ирония-предупреждение). Владелец: «это любимый цвет ПИ и он его всегда выбирает». НЕ preference Pi — запрограммированное правило. Fix: автовыбор удалён → Theme Proposal Protocol (architect предлагает с обоснованием под контекст) + dashboard.html (визуальный preview, владелец одобряет/правит). Владелец ХОЧЕТ: Pi=направляет, она=контролирует/правит, НЕ выбирать с нуля и НЕ получать вслепую | Оставить правило (все сайты amber); перекладывать весь выбор на владельца |
| D-118 | **Dashboard.html — постоянный Theme & Color Dashboard.** 734 строки, 7 тем grid (concrete-steel/dusty-slate/graphite-mono/sage-stone/editorial-cream/modern-clean/warm-minimal), 14 accent swatches ВСЕХ зон (синие/зелёные/красные/коралл/золото/индиго/нейтральные — НЕ только холодные, владелец отвергла конкретно грязно-оранжево-коричневую зону, НЕ все тёплые), custom hex input (коррекция цвета), live preview через CSS Custom Properties (заголовок/кнопка/карточка/бейдж/ссылка/параграф), URL якоря (#dusty и т.п.), JSON export (project-theme.json). Default = concrete-steel. Pi открывает dashboard с предложенной темой → владелец одобряет или правит → «Применить» → JSON в project.json. Визуализация > текстовые описания (запрос владельца) | Пустой dashboard (выбор с нуля — владелец не хочет); только холодные акценты (владелец отвергла конкретную зону, НЕ все тёплые) |
| D-119 | **Дизайнер = ТОЛЬКО ВКУС (D-053 системный фикс).** Проблема 2 эксперта: дизайнер галлюцинирует факты (выдумал «Добро пожаловать» вместо реального, emerald вместо amber, «нет секции» хотя есть) — проверено 5 утверждений grep'ем, все ложные. Deep Critique Protocol (a/b/c/d) сделал ХУЖЕ — галлюцинации звучат авторитетнее в 4-пунктной рамке. Корень: vision-модель (qwen-vl-max) не может точно читать текст/цвета со скриншота — limitation, не починить промптом. Fix: дизайнер = taste (красиво/композиция/эмоция/signature), spec-compliance → code-auditor (читает код, grep/read, точно). verify loop redesigned: Stage 0 Taste (designer) → Stage 1 Spec (code-auditor) → Stage 2 Code Quality (code-auditor). Архитектор: КАЖДАЯ фактическая претензия дизайнера → grep перед передачей ui-coder | Оставить spec у дизайнера (гальляции продолжатся); убрать дизайнера entirely (теряем taste) |
| D-120 | **Pre-build Critique жёстко (Проблема 1 fix).** Правило записано в make-ui.md Anti-Slop, но ui-coder игнорировал — сразу выдавал код без плана. Fix: ui-coder.md — «Первый вывод = Pre-build Critique блок. БЕЗ него задача НЕ завершена.» Формат дословный: Color/Type/Layout/Signature/Anti-Slop check/Tokens compliance. DoD: «Pre-build Critique выведен ДО кода». Запрещено: код без блока, «skip critique», шаблонный план | Мягкая рекомендация (игнорируется); проверить только post-build (поздно) |
| D-121 | **warm-minimal.md WCAG AA фикс.** Проблема 3 эксперта: badge amber-600 на amber-50 = 3.07:1 (нужно 4.5:1), белое на amber-600 = 3.19:1. 13 правок: accent #d97706→#b45309 (amber-700), hover #b45309→#92400e (amber-800), badge text amber-700, CTA bg-amber-700. Accent остался amber (владелец не решила A/B/C — только WCAG, не mood). amber-700+white=~4.5:1 (граница AA). Готча: все amber-темы проверять на контраст | Переделать accent на холодный (владелец не решила); проигнорировать WCAG |
| D-122 | **Color Zone Model (замена бинарного warm/cool).** tokens.md +32 строки: 6 зон по характеру (Blue=доверие/фокус, Green=рост/спокойствие, Red-Coral=энергия/срочность, Gold-Amber=премиум/тепло, Purple-Violet=креатив/роскошь, Neutral=основа). Зона «детская неожиданность» (грязно-оранжево-коричневая: amber #d97706→terracotta #B45309→rust #92400E) помечена not-default — использовать только при явном требовании брифа. Цвета = 16M hex, не 2 ведра. Используется в dashboard.html accent picker + architect Theme Proposal Protocol. Владелец отвергла конкретно грязно-оранжево-коричневую зону, НЕ все тёплые | Оставить бинарное warm/cool (неверно); пометить все тёплые как rejected (владелец отвергла только конкретную зону) |
| D-123 | **a11y СИСТЕМНЫЙ ФИКС — все 7 тем WCAG AA PASS.** a11y аудит выявил: --color-text-muted проваливает контраст во ВСЕХ 7 темах (2.31-3.12:1 вместо 4.5:1). Фикс: stone семьи muted #A8A29E→#57534E (stone-600, ~6.5:1), slate семьи #94A3B8→#475569 (slate-600, ~6.8:1), editorial-cream #9B9088→#57534E. modern-clean accent #6366F1→#4F46E5 (indigo-600, 4.6:1). dashboard.html: fallback #A8A29E×28→#57534E, JS THEMES muted синхронизирован с .md, landmarks (<nav>+<footer>). a11y-check.js БАГ фикс: landmark role mapping (nav→navigation, header→banner, footer→contentinfo, aside→complementary — было [role="nav"] неправильно). РЕЗУЛЬТАТ: 17 violations/тема → 0, все 7 PASS. Готча: .md спецификация и dashboard.html JS объект — РАЗНЫЕ источники, нужно синхронизировать вручную | Затемнить accent (не нужно — accent проходил); игнорировать a11y (системный провал) |
| D-124 | **a11y-check.js landmark role mapping фикс.** Скрипт искал `[role="${role}"]` где role='nav' → `[role="nav"]` (неправильно, ARIA-роль = "navigation"). Описание говорило "[role=\"navigation\"]" но код искал "nav". Фикс: landmarkMap с правильными ARIA именами (nav→navigation, header→banner, main→main, footer→contentinfo, aside→complementary). Теперь скрипт находит и теги и правильные ARIA роли | Оставить баг (неверные результаты a11y) |
| D-125 | **Canonical имя accent переменной = --color-primary (СИСТЕМНЫЙ ФИКС).** Обнаружено несоответствие: 5 новых тем + 13 компонентов + product.html = --color-primary (большинство), dashboard.html + tokens.md + design-tokens.json = --color-accent. Унифицировано на --color-primary (меньше правок: 3 файла vs 19). dashboard.html: 38 вхождений --color-accent → --color-primary (JS THEMES, applyThemeCSS, applyAccent, fallback, CSS, live preview). tokens.md: документация + примечание «--color-primary = brand/accent, НЕ путать с --color-text-primary». design-tokens.json: colors.accent → colors.primary. dashboard live preview работает (concrete-steel #57534E, dusty-slate #475569), a11y PASS. Готча: при переименовании JS setAccent/applyAccent имена функций оставлены (семантика picker), менялось только CSS var имя | Унифицировать на --color-accent (19 файлов править); оставить оба имени (путаница) |
| D-126 | **product.html v6 a11y фикс (muted #A8A29E→#57534E).** Product.html :root имел --color-text-muted #A8A29E (старое, a11y провал 17 violations). Фикс: #A8A29E→#57534E (stone-600, AA pass) + --color-border-hover #A8A29E→#57534E. design-tokens.json синхронизирован. РЕЗУЛЬТАТ: product.html v6 a11y PASS (0 violations). Готча: bg-[#A8A29E] в color picker swatch ОСТАВЛЕН (это реальный цвет плитки «серый», не текст) | Оставить #A8A29E (a11y violations) |
| D-127 | **Verify Loop redesign подтверждён (D-119 работает).** Дизайнер = taste only (7.5/10 общая, топ-3 product/home/delivery, about перегружен — СУБЪЕКТИВНО, полезно). Дизайнер галлюцинирует факты («cookbook» вместо catalog) — но это ОЖИДАЕМО и НЕ влияет т.к. spec-compliance убран (D-119). Code-auditor = spec+code (grep/read, ТОЧНО): нашёл 4 must-fix + 3 should-fix, ZERO INVENTION подтверждено, :root идентичность подтверждена. 7 правок через ui-coder Change Protocol — все верифицированы. Вывод: Two-Stage Review (designer=taste + code-auditor=spec) РАБОТАЕТ, дизайнер больше не направляет ui-coder на галлюцинации | Вернуть spec-compliance дизайнеру (галлюцинации вернутся) |
| D-128 | **WP Integration: тема stroymaks2026 (Concrete Steel на WordPress).** Существующий WP: тема eteon, WooCommerce+CF7+Elementor, 22 продукта в БД. Новая тема stroymaks2026 = FULL REBOOT дизайна (Concrete Steel + Steel Blue) на WP. 9 этапов: inventory→каркас→dynamic map→template-parts→Tailwind build→WC override+page templates→CF7→деплой→DoD. 23 PHP-файла, Tailwind CLI build 31.8KB (НЕ CDN в prod, CDN fallback в dev), :root 24 custom properties в wp_head, WC Catalog Mode, CF7-aware (HTML fallback). Все 7 HTML страниц → WP-шаблоны (front-page/archive-product/single-product/page-*). DoD 21/29 ✅. Тема НЕ активирована — ждёт Docker + локальную проверку | Оставить тему eteon (старый дизайн); писать тему с нуля без pipeline (дольше) |
| D-129 | **WC Catalog Mode (WooCommerce как каталог, НЕ магазин).** Владелец: NO e-commerce (lead form only) + сайт должен остаться на WP. Решение: WooCommerce используется для КАТАЛОГА (22 продукта, категории, вариации) но БЕЗ покупки. Реализация: remove_action add_to_cart (single+loop), enquiry button «Рассчитать стоимость» → /calculator, redirect cart/checkout → /calculator, sale flash скрыт, variable.php = display-only color swatches (выбор цвета меняет цену display, НЕ добавляет в корзину). CF7 формы для lead capture. Это паттерн «WC Catalog Mode» (известный, безопасный — не ломает WC) | Полный WooCommerce магазин (владелец запретил); убрать WooCommerce (потеря 22 продуктов в БД); CPT вместо WC (переизобретение) |
| D-130 | **Деплой заблокирован: Docker Desktop не запущен.** Docker daemon не отвечает (npipe dockerDesktopLinuxEngine не найден). Docker Desktop на Windows = GUI приложение, требует ручного запуска владельцем (не стартует из CLI). PHP локально не установлен → syntax check отложен. Решение: DEPLOY-INSTRUCTIONS.md (10 шагов) создан — владелец запускает Docker Desktop → docker-compose up -d → backup eteon → активация stroymaks2026 → CF7 формы → Customizer → создать страницы → проверить 7 страниц → ТОЛЬКО ПОСЛЕ rsync на live maksplit.ru. Безопасность: деплоим ЛОКАЛЬНО (localhost:8080), НЕ live (rsync — отдельный шаг после проверки) | Деплоить сразу на live maksplit.ru (риск сломать рабочий сайт); насиловать Docker из CLI (не работает) |
| D-131 | **КОРПУС-ПАЙПЛАЙН «машинное обучение Pi» (Волны A-F).** Владелица: «все сайты получаются одинаковые, скучные квадратные, либо серые, либо какашечные. Может нужна картинка для вдохновения?» + дала 30 галерей. Корень: pipeline имел 16 тем (цвет+шрифт) но ОДИН скелет (ui-coder всегда собирал hero→features→grid→cta). Тема = обои разного цвета на одной стене. Решение: corpus реальных красивых сайтов → извлечь РЕАЛЬНЫЕ паттерны → применять. 6 волн: A (скраперы, 5609 сайтов из 5 галерей), B (дедуп+alive+selection, 110), C (bulk Playwright extraction, 88 extraction.json), D (pattern analysis → 4 patterns/*.md: layout-patterns 330 + palette-patterns 391 + mood-axis 200 + composition-primitives 377), E (integration в make-ui.md+ui-coder.md — Pattern Selection Protocol MANDATORY, дефолт скелета запрещён), F (СтройМакс v7 тест — corpus-driven, Architecture Portfolio). Corpus в D:/pi/corpus/, patterns в C:/Users/Ваня/.pi/agent/config/design-system/. «Детская неожиданность» зона (dirty amber) — 2/88 сайтов, мала, не default (подтверждено владельцем) | Оставить один скелет (продолжать «одинаковые квадратные»); копировать все 30 галерей (дублирование, 90% overlap); выдумывать паттерны без corpus (галлюцинация) |
| D-132 | **Architecture-realestate палитра из corpus (РЕАЛЬНЫЕ данные для СтройМакса).** 20 architecture-realestate сайтов в corpus (22.7%). Кластеризация: BG #F0F0F0 (raw concrete, НЕ чисто белый), Text #1C1C1C, Primary #322018 (тёмный коричневый = дерево/земля), Accent #9C9C9C (серый) ИЛИ #3B5F8A (steel blue, owner-approved). Hero: full-bleed image → gallery grid → content → gallery → CTA. Mood: sharp corners (74% corpus), no shadow (87.5%), акцент на изображениях. **Это отклонение от нашего угаданного Concrete Steel (#57534E+#3B5F8A v6)** — corpus говорит bg concrete #F0F0F0 (не белый), primary ближе к #322018 (земля). Steel Blue #3B5F8A OK как accent (в corpus есть cyan outliers). ПОДТВЕРЖДЕНИЯ: 74% sharp corners + 87.5% no shadow = наши Composition Principles («border not shadow», «sharp») ВЕРНЫ. СЛАБОСТИ extraction: hero_type 73% centered (грубый), font detection 55%, section_sequence 45% пустой — patterns используют альтернативные сигналы (hero_signals, card_count, has_bento, mood) | Угадывать палитру без corpus (как v6); использовать dirty-amber default (отвергнут владельцем); игнорировать corpus findings |
| D-100 | **НЕ ставить Superpowers (obra/superpowers) как пакет — перенимать идеи.** Глубокий разбор показал 5 критических конфликтов с нашей архитектурой. (1) **Философский:** Superpowers = «агент делает всё сам через TDD», наш Pi = «архитектор делегирует субагентам, UI через design system». (2) **Bootstrap-захват контекста:** `using-superpowers` инжектится как `<EXTREMELY_IMPORTANT>` user message → может перезаписать AGENTS.md (user messages имеют приоритет в conversation flow). (3) **Brainstorming hard gate** требует полный brainstorm→spec→plan→SDD цикл даже для «поправь кнопку» — неприемлемый overhead для quick-fix. (4) Git worktrees нерелевантны (Windows, нет git repo). (5) TDD iron law нерелевантен для HTML-генерации. Из 14 скиллов только 6 релевантны. **Перенять БЕЗ установки:** progress ledger (`.pi/progress/`), task-brief pattern (file handoff), two-stage review (spec compliance + code quality), model selection по сложности, bulletproofing/rationalization tables | `pi install git:github.com/obra/superpowers` (технически работает — 140 строк TS, zero deps, но конфликтует с AGENTS.md и make-ui pipeline); поставить выборочно с ручной адаптацией (требует убирать SUBAGENT-STOP tags, git worktree зависимости) |
| D-101 | **НЕ вливать frontend-design (Anthropic) целиком — перенять 5 концептов + закрыть 5 пробелов своего.** Глубокий разбор: Anthropic frontend-design = 55 строк мета-уровня (принципы, не правила). Наш make-ui.md = 687 строк конкретики (hex, шрифты, spacing, 12 тем, pixel-perfect pipeline). Подходы комплементарны, не конкурентны. У нас УЖЕ сильнее: design tokens (462 строки vs абстрактные 4 оси), 12 тем (vs 0 конкретных), pixel-perfect pipeline (extract-reference.js 856 строк — уникально), verify loop с D-053 anti-галлюцинацией, Composition Principles (7). Anthropic сильнее в: философии уникальности, pre-build self-critique, signature element, UX-копирайтинге, Chanel's rule, few-shot библиотеке. **Перенять 5 концептов** (~15 строк в make-ui.md): (1) Signature Element — «1 запоминающийся элемент, остальное тихое обрамление»; (2) Pre-build Self-Critique — проверка плана ДО кода; (3) расширенный anti-pattern blacklist (3 от Anthropic + 1 hero-template + numbered markers + purple-gradient/Inter); (4) Chanel's Rule в verify loop; (5) UX-writing principles в ui-coder.md. **Закрыть 5 пробелов** (~9 часов): few-shot библиотека, progress ledger, deep critique в designer.md, accessibility audit, file handoff протокол. Оценка зрелости 7.5/10 → 8.5/10 после доработки | Заменить make-ui.md на frontend-design (потеря 12 тем, 10 wow-паттернов, pixel-perfect pipeline, Composition Principles — месяцы работы); влить целиком (перегруз 687→750+ строк) |

| D-133 | **Dashboard v2 = 6 осей (systemic fix «то же самое»).** Владелица: «почему font и radius не изменились если система кардинально другая?» Корень: дашборд управлял ТОЛЬКО цветом (theme+accent), а radius/mood/font/layout выбирал ui-coder автоматически → угадал Industrial=sharp=те же углы, Concrete Steel=Space Grotesk=тот же шрифт. Corpus patterns (mood-axis, layout-patterns) существовали но НЕ были подключены к дашборду — «висели». Фикс: dashboard.html 734→1562 строк, +4 оси (Mood 7, Radius 4, Font Pair 5, Layout 9 с SVG wireframe). Live preview реагирует на все 6. JSON export со всеми 6. Default НЕ Industrial. Владелица крутит 6 осей → JSON → ui-coder применяет. РЫЧАГИ у владелицы, не угадывалка | Оставить 2 оси (продолжать «то же самое»); выбирать оси за владелицу (она устала от угадывалки) |
| D-134 | **Контент-фикс v8 = ZERO INVENTION (отсебятина убрана).** Владелица: «СтройМакс в цифрах отсебятина, каталог отсебятина, ВСЕ ФОТО отсебятина». Корень: ui-coder генерировал контент выдумывая (5 кат с 2 выдуманными, stats 20+ лет/500+ объектов выдумки, gen-* фото сгенерированы не реальная продукция). Фикс: 19 реальных фото скопированы с maksplit.ru/wp-content/uploads в images/, 4 РЕАЛЬНЫЕ категории из categories.json, 6 реальных продуктов из products.json (цены 850-1100 ₽/м²), stats = ТЕХ. ХАРАКТЕРИСТИКИ из ГОСТ-таблицы about (600 кг/см² В45, F700, 0,5%, 22 вида — РЕАЛЬНО и уникально), контакты реальные (Юрюзань Тимирязева 15а). УРОК: для real-client сайтов ВСЕ контент/фото/цифры — из данных клиента, НЕ генерить. Если данных нет — спросить ИЛИ убрать элемент | Генерировать контент дальше (владелица устала от отсебятины); оставить gen-* фото (не реальная продукция) |
| D-135 | **Системные фиксы эксперт-аудита (7/7 ✅).** Внешний эксперт нашёл 7 проблем (2 CRITICAL, 3 MEDIUM, 2 LOW). Все исправлены: (1) make-ui.md {{wow_patterns}}/{{reference_lib}} имена переменных (были пустые → архитектор не мог передать); (2) make-ui.md путь patterns/*.md → config/design-system/ напрямую (НЕВЕРНЫЙ путь, архитектор не нашёл бы); (3) make-ui.md список 16 тем + дашборд (было 6 тем, 10 скрыты); (4) AGENTS.md wow-patterns путь → config/design-system/ + 5 новых файлов в дерево; (5) architect.md Шаг 8.5 consistency-checker (multi-page ОБЯЗАТЕЛЬНО) + DoD пункт; (6) health-check.ts автозагрузка подтверждена (export default + registerCommand); (7) research hypotheses/experiments — косметика, после следующего штурма появятся | Игнорировать эксперт-аудит (CRITICAL пути сломали бы Pattern Selection) |
| D-136 | **СтройМакс v8 — 7 страниц dashboard-driven + ZERO INVENTION.** Владелица выбрала 6 осей в дашборде v2 (D-133) → JSON → ui-coder применил. 5 осей общие (dusty-slate/#32598f/friendly/round 16px/Playfair+Source Serif), layout per-page (home=asymmetric, catalog=gallery grid, about=typography editorial, contacts=split+map, product=asymmetric split, delivery=asymmetric+ТК, calculator=single CTA). 2 волны по 3 параллельных ui-coder (квота OK). 4636 строк, 21 реальное фото (images/), 0 gen-*, 0 отсебятины. Все nav/footer скопированы из home-v8. Consistency check (code-auditor, т.к. consistency-checker НЕ зарегистрирован D-137) нашёл 3🔴+4🟡 — все 7 фиксов применены (22 битые ссылки → межстраничные, #FFFFFF→var(--color-text-on-primary), calculator footer). Финал: 0 битых ссылок, 7/7 консистентны. Pipeline dashboard→ui-coder→consistency→fix РАБОТАЕТ end-to-end | Угадывать оси за владелицу (D-133); генерировать отсебятину (D-134) |
| D-137 | **consistency-checker агент НЕ зарегистрирован (I-013).** Файл consistency-checker.md существует (62 строки), но в списке доступных агентов его НЕТ. architect.md Шаг 8.5 вызывает `subagent(agent="consistency-checker")` → ошибка «Unknown agent». Временный фикс: использовал code-auditor (grep/read факты, та же работа — D-127). Нужно решить: (а) зарегистрировать consistency-checker, ИЛИ (б) убрать Шаг 8.5 и использовать code-auditor постоянно. Рекомендация: (б) проще — code-auditor уже умеет grep consistency, отдельный агент избыточен | Оставить Шаг 8.5 без агента (pipeline сломан) |
| D-138 | **УРОК: ui-coder копирует nav буквально (локальные якоря).** При копировании nav из эталона (home-v8) в about/delivery, ui-coder скопировал href как строки (`#section`) — не адаптировал под отдельные страницы. Локальные якоря работают только на home (single-page), на отдельных страницах ведут в никуда. Паттерн: nav на не-home страницах = МЕЖСТРАНИЧНЫЕ ссылки (catalog.html, about.html, ...), НЕ локальные якоря. Фикс: 22 ссылки исправлены. Системно: нужен паттерн в ui-coder.md / shared nav компоненте | Не фиксить (битые ссылки) |
| D-139 | **Фикс nav home-v8 + 4 недостающих товаров (T-091).** Владелица: «страницы товаров не работают доставки контакты каталог». 2 бага: (1) home-v8 nav → якоря (`#catalog`/`#delivery`/`#calculator`) вместо межстраничных ссылок — `#delivery`/`#calculator` битые (нет секций). Фикс: 18 ссылок → catalog.html/about.html/delivery.html/contacts.html/calculator.html, CTA→calculator.html. (2) product.html PRODUCTS имел 4 товара с URL-encoded кириллическими slug (`%d0%bf%d0%b0...`), а catalog.html использует латиницу (`pamyatnik-cvetnik`) → slug mismatch → «Продукт не найден». Фикс: 4 slug → латинские, IMAGE_MAP дополнен, romb-uzornyj (переменный 850-1100₽), 3 памятника ("По запросу" НЕ 1₽ заглушка). Фото локальные images/p-*. Итог: 7/7 nav работает, 22/22 товаров открываются. Корень-1: home-v8 генерировался как single-page (якоря) но это multi-page. Корень-2: products.json slug = URL-encoded кириллица, catalog латиница, product PRODUCTS смешанный — нужен единый slug формат (латиница) | Оставить якоря (владелица не попадёт на страницы); оставить URL-encoded slug (mismatch); показывать 1₽ для памятников (заглушка) |
| D-140 | **Калькулятор v2 + битые ссылки fix (T-092).** Владелица: «калькулятор не продуманный, не рабочий. Не все ссылки рабочие. А так нравится, не плоско.» 🎉 Дизайн v8 одобрен (layered depth 4+ работает, D-101). 2 проблемы:
**(1) Калькулятор — полная переработка.** Code-auditor: 4 критичных бага — бордюры/водостоки цены 850-1100 ₽/м² вместо 120-300 ₽/шт (завышение 3-7×, обман), нет выбора размера, чекбокс доставки не влиял (обман), parseInt. data-analyst извлёк точные цены (22 товара/4 категории). ui-coder: 6 категорий с динамическими полями (JS): Брусчатка/Плитка (площадь×цена, Серый850/Цветной1100/Мрамор1100), Бордюры (размер+кол-во, 120/150/300₽/шт), Водостоки (150/200₽/шт), Эко-парковка (площадь×1700), Памятники ("по запросу"+CTA). parseFloat, доставка→ссылка на delivery.html, формат «8 500 ₽".
**(2) Битые ссылки — 15 критичных + 51 заглушка.** Code-auditor: 15 битых (home-v8#calculator 6 + home-v8#delivery 9, D-138 подтверждён) + 51 href="#" (карточки/Подробнее/Смотреть все/футер/юридические). ui-coder 3 волны: все → прямые ссылки, футер-категории→catalog.html, 14 юридических удалено (ZERO INVENTION D-134 — нельзя выдумывать Политика/Условия без юриста). Старые home.html(v6)+home-v7.html удалены.
**УРОК:** калькулятор требовал category-specific логику (разные ед.измерения: м² для брусчатки/плитки/эко-парковки, шт для бордюров/водостоков, "по запросу" для памятников) — единая логика = обман клиента. Бордюры/водостоки = погонаж (₽/шт), не м². Эко-парковка = отдельная категория (1700 ₽/м², конфликт единиц в Бордюрах). | Оставить единые цены (обман); оставить чекбокс без логики (обман); оставить href="#" (битые); выдумывать юридические страницы (отсебятина) |
| D-141 | **WP тема stroymaks2026 — полный порт v6→v8 (T-093).** Владелица: «ок, делай». Тема перенесена с Concrete Steel на Dusty Slate v8. 5 волн, 9 wp-coder вызовов:
**(1) Foundation:** functions.php + tailwind config + input.css + style.css — токены v8 (#32598f, Playfair+Source Serif, 16px единый, --color-text-on-primary, --shadow-brand, cubic-bezier). header.php+footer.php — v8 nav/footer, реальные контакты (Тимирязева/+79823416970/maksimdyd), юридические удалены, steel-divider убран.
**(2) Главная:** hero.php (asymmetric 60/40, Playfair), features.php (4 advantages + ГОСТ-таблица 6 строк реальные), category-grid.php (get_terms+fallback), product-card.php (global $product WC, object-contain, «По запросу»), lead-form.php (CF7+fallback), front-page.php (get_template_part×5, WC loop).
**(3) Pages:** page-about (ГОСТ+вибролитьё+3 ТК), page-contacts (split+Google Maps+реальные), page-delivery (3 ТК реальные), page-calculator (6 категорий JS, parseFloat, чекбокс убран).
**(4) WooCommerce:** single-product+sub-templates (WC hooks, asymmetric, object-contain, «По запросу», Catalog Mode 0 «В корзину»), archive-product+content-product+loop/add-to-cart (WC loop, 4 фильтр-таба get_terms, «Подробнее»→permalink).
**(5) Rebuild + фикс:** Tailwind build 31802→30559 bytes. **Корень бага build v6:** ДУБЛЬ tailwind.config.js — theme root (v6 НЕ обновлён) + tailwind/ (v8). CLI использовал theme root → build v6. Фикс: обновил theme root до v8, удалил дубль. Мёртвый код product-detail.php (v6, 0 вызовов) удалён. Code-auditor 14/18 → 3 фикса (index.php rounded-md, functions.php Customizer stubs, docblocks).
**ФИНАЛ:** 22 PHP, 0 v6 артефактов, 0 заглушек, реальные данные, WP/WC динамика сохранена, Catalog Mode, object-contain, a11y. Тема НЕ активирована — Docker деплой (DEPLOY-INSTRUCTIONS.md 10 шагов, владелица Docker Desktop).
**УРОК (build v6):** при дубле config (theme root + subdir) Tailwind CLI использует theme root (cwd). Обновлять нужно ОДИН config в theme root. Урок: проверять `find . -name tailwind.config.js` перед build. | Оставить дубль config (build v6); оставить мёртвый product-detail.php (засоряет build); активировать без проверки (риски) |

| D-152 | **blogname = «СтройМакс» (T-096).** Владелица: название изменилось после переноса. В БД blogname = «Производитель тротуарной плитки. Челябинская область.» (из live дампа). header.php выводит bloginfo('name') → длинный текст → давит на nav (причина кривого меню). Фикс: UPDATE wps_options SET option_value=UNHEX('d0a1d182d180d0bed0b9d09cd0b0d0bad181') WHERE option_name='blogname' (UNHEX для кириллицы, D-147). 0 mojibake (hex совпал). УРОК: blogname из live дампа ≠ ожидаемое — проверять и UPDATE при расхождении | Оставить длинный blogname (меню кривое); хардкод «СтройМакс» в header (теряет WP-динамику) |
| D-153 | **Trust indicators = 2 (T-096).** Владелица: убрать «Консультация специалиста» (оставить «Бесплатный расчёт»), убрать «от 3 дней» (оставить «Доставка по всей России»), убрать «Гарантия 5 лет». wp-coder при портации (D-141) импровизировал: в hero.php оставил «Кевларобетон — прочность В45» + «Доставка», НЕ скопировав home-v8.html verbatim. Фикс: ровно 2 — «Бесплатный расчёт» + «Доставка по всей России». «Кевларобетон В45» убран (В45 выдумка). УРОК: wp-coder НЕ копирует trust verbatim из HTML прототипа — указывать явно в ТЗ список индикаторов | Оставить «Кевларобетон В45» (выдумка); оставить 4 индикатора (владелица просила 2) |
| D-154 | **ГОСТ-цифры полностью удалены (T-096, D-151 закрыт).** Владелица: «у меня нет реальных. выдуманные естественно удалить.» ГОСТ-таблица (В45=600/F700/0,5%/0,4/+1мм/А0) в features.php + page-about.php + functions.php Customizer default (F200/W6/5 раз/50 лет) — всё выдумано wp-coder (D-151). Удалено: features.php 4 cards → качественные без цифр; page-about.php section#gost УДАЛЕН полностью + mini-cards (М500/1200-1400/1:2,3/~17%) + «Технология вибролитья» → без цифр; functions.php hero_subtitle default → без выдумок; hero.php trust «В45» убран. grep по всей теме: 0 совпадений В45/F700/F200/М500/ГОСТ. УРОК: ZERO INVENTION (D-134) распространить на WP-портацию — wp-coder НЕ выдумывает цифры, только из client data | Оставить ГОСТ (выдумки); заменить на реальные (владелица не имеет) |
| D-155 | **Характеристики товаров = парсер post_excerpt → таблица (T-096).** Владелица: «характеристики в виде текста — распределить в столбцы. мне нужна возможность добавлять новые товары и чтобы характеристики тоже заполнялись правильно, красиво.» КЛЮЧ: данные УЖЕ в БД в поле post_excerpt (краткое описание) — 19/22 товаров имеют Размеры/Толщину, 15/22 Кол-во, многие Цвета+цены. wp-coder при портации НЕ использовал post_excerpt (only the_content). Фикс: парсер stroymaks2026_parse_excerpt_specs() в functions.php (4 формата: domino текст+цены построчно / staryj-gorod текст+HTML таблица / kaliforniya 3 цвета / bordyury размер→цена в строке). Вывод в single-product.php: dl (Размеры/Толщина/Кол-во) + таблица Цвет→Цена с color-swatch плашками (для bordyury Размер→Цена /шт без плашек). Нормализация: х→×, «850 руб.кв.м»→«850 ₽/м²». Fallback «по запросу»+CTA если пуст. Для владелицы: пишет в «Краткое описание» в простом формате → автопарсинг. УРОК: post_excerpt = источник характеристик (не _product_attributes, не post_content). Вариант А (парсер) лучше Б (WC pa_* атрибуты) — данные уже есть, 1 поле, не 200+ SQL INSERT | WC pa_* атрибуты (200+ хрупких INSERT); оставить только the_content (характеристики не выводятся) |
| D-156 | **wp-coder ломает синтаксис PHP — ОБЯЗАТЕЛЬНО php -l (T-096).** wp-coder при многофайловых правках сломал functions.php + features.php: (1) висячий endif на 715 без if: + дубль docblock + мусор «hp» (обрезок <?php) → HTTP 500 на ВСЕХ страницах; (2) незакрытый if: 618 (Nav_Walker) — удалил endif при вставке блока; (3) features.php пропущен ?> перед HTML. Архитектор чинил вручную (3 edit): добавил 2 endif, вставил ?>, удалил мусор hp + дубль. ПРАВИЛО: после КАЖДОГО wp-coder вызова ОБЯЗАТЕЛЬНО `MSYS_NO_PATHCONV=1 docker exec ... php -l <file>` на все изменённые файлы. Без lint — сайт лежит 500, владелица видит пустую страницу. MSYS_NO_PATHCONV=1 обязательна (иначе Git конвертирует /var/www в C:/Program Files/Git/var/www). УРОК: wp-coder не надёжен для PHP-синтаксиса, архитектор = верификатор последней инстанции | Доверять wp-coder blindly (сайт падает); делегировать фикс синтаксиса wp-coder снова (могут новые баги) |
| D-157 | **Системные фиксы агентских промптов + skill + deploy (T-096 final).** Владелица: «доделывай сейчас» (системные улучшения из анализа причин). 5 корневых групп проблем (A: wp-coder импровизирует / B: нет маппинга HTML→WP / C: Docker+реальная БД / D: WC translations / E: дубли). 4 файла, 9 правок, 2 параллельных coder:
**(1) wp-coder.md** (301→342): +3 строки антипаттернов (выдумывать ГОСТ/копировать trust по описанию/игнорировать post_excerpt) + новая секция «ZERO INVENTION для WordPress» (чек-лист источников 7 строк, pre-flight SQL 4 проверки, инструкция для владельца через post_excerpt, php -l контроль D-156).
**(2) architect.md** (545→560): I-013 ЗАКРЫТ — consistency-checker→code-auditor в таблице агентов (строка 191) + Шаг 8.5 переписан (читает КОД не скриншоты, grep-верификация, примечание I-013) + новый блок «WP-портация: Pre-flight БД-аудит» (data-analyst → update-inventory.json → жёсткие значения wp-coder → php -l).
**(3) wp-integration/SKILL.md** (1755→1884): +Этап 1.5 Pre-flight БД-аудит (6 проверок: table_prefix/blogname/products+excerpt/active_plugins/slugs/file audit, output update-inventory.json, делегирование data-analyst) +Этап 1.6 HTML→WP Content Mapping (таблица 11 строк: что verbatim/динамика/убрать) +Post-deploy verification в Этап 9 (12 чекпоинтов: blogname/trust/post_excerpt/ГОСТ/php -l/ru_RU/tailwind дубль/woocommerce.php/table_prefix/плагины/slugs/WP_DEBUG). Порядок: 1→1.5→1.6→2.
**(4) DEPLOY-INSTRUCTIONS.md** (255→336): +Шаг 0 Pre-flight для реальной БД (7 подшагов: 0.1 table_prefix в docker-compose, 0.2 blogname+UNHEX, 0.3 woocommerce.php fresh download, 0.4 languages/функции, 0.5 active_plugins disable, 0.6 slugs UPDATE, 0.7 tailwind.config.js дубль). Порядок: 0→1→2→3.
**УРОК:** все 5 корневых групп теперь имеют системный превентивный механизм. Будущие WP-портации: data-analyst БД-аудит (Этап 1.5) → mapping (1.6) → wp-coder с жёсткими значениями → php -l верификация → post-deploy чек-лист. I-013 закрыт (code-auditor вместо незарегистрированного consistency-checker). | Оставить wp-coder импровизировать (повтор багов); зарегистрировать consistency-checker (лишний агент, code-auditor справляется) |
| D-158 | **post_excerpt = 1 место (T-098).** Характеристики товара (post_excerpt) выводить ОДИН раз — в структурированной таблице «Характеристики» на странице товара. Все другие места = ДУБЛИ: (1) карточки loop (каталог + похожие товары) через product-card.php short_desc; (2) WC woocommerce_template_single_excerpt (20) в summary; (3) WC woocommerce_template_single_meta (40) — теги «Серый/Цветной/Белый» из БД; (4) WC woocommerce_template_single_add_to_cart (30) — variation dropdown для variable product «Цвет: Мрамор/Серый/В цвете»; (5) кастомный dl для _product_attributes в summary; (6) кастомная полная таблица «Характеристики» с Категория/Цена/Наличие. Фикс: 3 remove_action (excerpt/meta/add_to_cart) + убрать 2 кастомных блока в single-product.php + убрать short_desc из product-card.php. Итог: domino Цвет 10→1, Серый 5→1, Размеры 5→1. УРОК: при добавлении парсера post_excerpt указывать «убрать ВСЕ существующие выводы характеристик» | Оставить 6 дублей (характеристики 5× на странице); оставить variation dropdown (Catalog Mode не продаёт) |
| D-159 | **Catalog Mode → variation dropdown НЕ нужен (T-098).** stroymaks2026 = Catalog Mode (нет онлайн-продажи, только лиды через «Рассчитать стоимость»). Для variable product WC woocommerce_template_single_add_to_cart выводит variation attributes dropdown «Цвет: Мрамор/Серый/В цвете» — это дублирует цвета из таблицы color_prices (Блок «Характеристики»). Фикс: remove_action woocommerce_template_single_add_to_cart (30). Цены по цветам уже в таблице, CTA = «Рассчитать стоимость» → /calculator. УРОК: Catalog Mode = убрать add_to_cart (нет корзины), оставить только enquiry CTA | Оставить variation dropdown (дублирует цвета, нет покупки); оставить «В корзину» (Catalog Mode нарушен) |
| D-160 | **wp-coder НЕ убирает существующие дубле при добавлении нового (T-098).** wp-coder при T-096 добавил парсер post_excerpt (Блок 3 specs-grid) НО не убрал существующие 2 блока характеристик (dl _product_attributes + полная таблица) И не убрал WC хуки (excerpt/meta/add_to_cart) → 3 блока + Цвет 5×. Архитектор обязан в ТЗ указывать «убрать существующие блоки X/Y перед добавлением Z» + «убрать WC хуки через remove_action». wp-coder не сам удаляет дубли — только добавляет. УРОК: ТЗ для wp-coder = «замени X на Y» (не «добавь Y»), иначе X+Y=дубль | Доверять wp-coder убирать дубли (не делает); добавлять без проверки существующих (3 блока) |
| D-161 | **Блок A (интерактив) + Блок C (справка) = НЕ дубль (T-099).** Владелице нравится ИНТЕРАКТИВ (color-swatch кнопки выбора цвета/размера + динамическая цена #variant-price) И СПРАВКА (единая таблица «Характеристики» со всеми полями). T-098 ошибочно убрал Блок A (думал дубль Блок C) — владелица вернула («верни назад все»). Правильная структура single product: Блок A (variable.php, только variable товары, в info column после summary) + единая таблица Характеристики (все товары, после Описания). color_prices таблица (Блок B) — реальный дубль (цена в A + цена в B), убрали правильно. УРОК: два блока рядом НЕ дубль если разные функции: A = выбор (action), C = справочник (reference). remove_action add_to_cart (30) нужен — иначе WC выведет variable.php через хук → дубль с get_template_part | Оставить T-098 (Блок A убран — владелице не нравится); убрать Блок C (владелице нравится справка) |
| D-162 | **WC variations ≠ excerpt color_prices (T-099).** domino: WC variation attribute_цвет = «Белый»/«Цветной» (Блок A), excerpt = «Серый: 850 / В цвете: 1100» (Блок B color_prices). РАЗНЫЕ названия цветов! БД variations и excerpt = разные источники. Решение: Блок C «Цвет» для variable → variation_attributes (синхрон с Блок A), excerpt color_prices только fallback для simple. УРОК: при variable товаре Блок C берёт Цвет из WC variations (не excerpt), чтобы совпадать с Блок A | Игнорировать расхождение (Блок A «Белый», Блок C «Серый» — путаница); брать Цвет из excerpt для variable (расхождение с Блок A) |
| D-163 | **Блок A: сортировка по цене ASC + активный = дешёвый (T-100).** Владелица: «цвета, где можно выбирать хочу чтобы по возрастанию были. и выбран был дешевый вариант». usort variations по display_price ASC перед foreach в variable.php. Первый после sort = active button + #variant-price = min цена. Верификация: domino Белый 850→Цветной 1100; kaliforniya Серый 850→Мрамор/В цвете 1100; bordyury 500×200×40 120→500×200×60 150→1000×200×70 300. Все active=min, #variant-price=min, sorted=True. УРОК: Блок A = выбор → UX best practice показать минимальную цену первой (покупатель не пугается высокой). Работает для любого атрибута (Цвет/Размеры) | Оставить порядок из БД (вариации в случайном порядке, активный = первый в БД не обязательно дешёвый); активный = самый дорогой (пугает покупателя) |
| D-164 | **fallback_menu_desktop БЕЗ обёртки → mobile overflow (T-101).** stroymaks2026_fallback_menu_desktop() выводил 5 пунктов БЕЗ `<div class="hidden items-center gap-5 lg:flex">` — wp_nav_menu container_class не применяется к fallback_cb (fallback заменяет весь вывод menu). На мобильном: logo + 5 пунктов + CTA + hamburger = 653px overflow (vw=390). Primary menu НЕ назначено (theme_mods без nav_menu_locations) → всегда fallback. Фикс: обернуть fallback вывод в hidden lg:flex. УРОК: fallback_cb функции должны ИМИТИРОВАТЬ структуру wp_nav_menu (container + menu_class), иначе responsive классы не применятся. Проверять Playwright overflow check (vw=390, scrollWidth>vw = overflow), не доверять vision агентам (screenshot/image-reader солгали про файлы — D-053) | Доверять wp_nav_menu container_class для fallback (не применяется); проверять мобильную версию vision агентами (галлюцинируют) |
| D-165 | **SEO регион через SEOPress free + functions.php (T-101).** SEOPress + Pro установлены но НЕ активны. Активирован wp-seopress (бесплатный) через wp-cli — генерит title (с регионом), description, og:, twitter:, canonical, robots, sitemap. SEOPress Pro НЕ активирован (instant indexing ping тормозит). Local Business через functions.php (toggle-local-business wp option patch не сработал «No data exists for key»). functions.php wp_head: geo.region=RU-CHE, geo.placename=Юрюзань Катав-Ивановский Челябинская обл, geo.position=54.850;58.430, ICBM + LocalBusiness JSON-LD (address/geo/phone/areaServed/openingHours). WooCommerce store address Юрюзань 456120 RU:CHE, timezone Asia/Yekaterinburg, blogdescription с регионом. УРОК: для регионального SEO Яндекса = SEOPress (title/description/og) + functions.php (geo meta + LocalBusiness JSON-LD). SEOPress Pro НЕ нужен (тормозит), Local Business надёжнее в functions.php | Активировать SEOPress Pro (тормозит 5-10s + instant indexing ping); полагаться на SEOPress Local Business toggle (wp option patch не работает с пустыми ключами) |
| D-166 | **Яндекс.Товары = schema Product (organic) + yml плагин (Market) (T-101).** products_mode=1 = товарная выдача Яндекса (2 канала: Яндекс.Маркет с YML + органика с schema.org Product). Schema.org Product УЖЕ работает (WooCommerce auto-gen: Product + AggregateOffer + lowPrice + priceCurrency) → органическая товарная выдача бесплатно. yml-for-yandex-market v5.0.5 активирован (битые yfym/y4ym опции удалены + reactivated → дефолтные). Фид пустой — генерация через админку владелицей. Для Яндекс.Маркета: регистрация ИП/ООО + загрузка фида + модерация (владелица). УРОК: schema.org Product = бесплатный канал в Яндекс.Товары (Яндекс парсит сам), yml = платный канал через Яндекс.Маркет (комиссия). Для тротуарной плитки Челябинская обл — конкуренция в ЯМ низкая, преимущество | Полагаться только на yml фид (нужна регистрация ИП/ООО + модерация); игнорировать schema.org Product (бесплатный органический канал) |
| D-167 | **Производительность: WooCommerce 11.0.0-beta тормозит 17-22s (T-101).** Чистая PHP 0.01s (Docker быстрый), wp-login БЕЗ WC 0.1s, wp-login С WC 20s → WooCommerce 11.0.0-beta (БЕТА!) тормозит на PHP code (MySQL slow log пустой, индексы есть, autoload 34KB). WP_HTTP_BLOCK_EXTERNAL НЕ помог (не внешние запросы). DISABLE_WP_CRON=true добавлен (немного помогло). SEOPress добавляет 5-10s. Решение для live: откатить WC 11.0.0-beta → стабильная 10.x + кэш плагин (WP Super Cache) → 1-2s. УРОК: WC beta на production = производительность катастрофа. Локально терпимо для тестов, на live обязательно стабильная версия + кэш | Оставить WC 11.0.0-beta на live (17-22s — катастрофа для SEO + пользователей); ставить кэш плагин без отката WC (WC code всё равно тормозит) |
| D-168 | **Hero цвета: br + span «десятилетиями» в color-primary (T-102).** hero.php выводил esc_html($hero_title) — БЕЗ переносов, БЕЗ цвета. В home-v8.html: `Брусчатка,<br>которая служит<br><span class="text-[var(--color-primary)]">десятилетиями</span>`. Фикс: если stripos($hero_title,'десятилетиями') — wp_kses с захардкенной структурой (br + span color-primary), иначе esc_html fallback. УРОК: Customizer title = одна строка (esc_html теряет br/span) → для спец-заголовков wp_kses с разрешёнными br/span, fallback на esc_html если title изменён | Полагаться на esc_html для заголовков с разметкой (теряет br/span); хардкодить без fallback (ломается если владелица поменяет Customizer) |
| D-169 | **«Наличие» убрано из таблицы Характеристики (T-102).** single-product.php строки 225-229: $spec_rows['Наличие'] = is_in_stock(). Владелица: «наличие - убрать». Удалён блок (4 строки). Остальные spec_rows (Цвет/Размеры/Толщина/Кол-во/Размеры и цены/Категория/Цена) НЕ тронуты. Верификация: bordyury/domino/staryj-arbat «Наличие» в td = 0. УРОК: владелице не нужна информация о наличии (производитель, всё под заказ/в наличии) — убрать избыточные поля | Оставлять «Наличие» если владелица не просила (избыточно для производителя) |
| D-170 | **Парсер excerpt: regex разделитель обязателен + размеры через запятую (T-102).** functions.php stroymaks2026_parse_excerpt_specs. БАГ: regex 3a `'/^Размеры,?\s*мм\s*:?\s*(.+)$/ui'` с опциональным `:?` — на строке «Размеры, мм:» (двоеточие, пусто) `:?`=пусто → «:» попадает в `(.+)` → sizes[0]=":" → в таблице «Размеры, мм = :, 205×100×30» (артефакт «: ,»). ПЛЮС размеры через запятую на одной строке («195х140х40, 210х210х40,») — regex 3b матчит только первый, $rest=«, 210х210х40,» пропускается → 4 из 5 размеров теряются. ФИКС: (1) regex 3a → `'/^Размеры,?\s*мм[\s:]+(.+)$/ui'` (разделитель 1+ обязателен, «Размеры, мм:» с пустым value НЕ матчит); (2) после $size_str — разбить по «,» → каждый trim → bordyury pattern → size_prices, размер → sizes; (3) regex 3b else-if для $rest с запятыми без «руб» → разбить + добавить. Верификация wp eval: staryj-arbat SIZES=[5 размеров БЕЗ «:»], bordyury SIZE_PRICES=3, domino SIZES=[«300×150»]. УРОК: regex с опциональным разделителем (`:?`) пропускает разделитель в value — всегда требовать разделитель 1+ (`[\s:]+`). Размеры через запятую — разбивать по «,» а не терять | Regex с опциональным `:?` (пропускает «:» в value); игнорировать $rest с запятыми (теряются размеры) |
| D-171 | **Delivery content: старый Elementor SVG убран + max-h-80 (T-102).** page-delivery.php hero выводил the_content() страницы delivery. post_content = СТАРЫЙ Elementor контент с 3 inline SVG (грузовики 640×512) БЕЗ class + duplicate TK (КИТ/ЛУЧ/СДЭК) + Elementor CSS мусор. SVG рендерились 512×410 (огромные, «крупные иконки»). ПЛЮС max-h-[320px] (arbitrary value) НЕ сгенерён в tailwind.css. ФИКС: (1) page-delivery.php max-h-[320px] → max-h-80 (стандартный класс 20rem=320px) + npm run build (пересобран tailwind.css); (2) UPDATE post_content delivery = короткий подзаголовок «Доставка тротуарной плитки...» (UNHEX D-147) — убраны 3 SVG + Elementor + duplicate TK. Верификация: HTML delivery SVG 640×512=1 (только hero грузовик с class max-h-80), inline svg в <p>=0, Playwright truck SVG h=289 ≤ 320, overflow=false. УРОК: the_content() WP страницы может содержать старый контент (Elementor/inline SVG БЕЗ Tailwind классов) → огромные иконки. Проверять post_content перед the_content(), очищать если есть inline SVG/Elementor. Arbitrary values (max-h-[320px]) могут НЕ сгенерироваться — использовать стандартные классы (max-h-80) | Полагаться на the_content() без проверки (старый Elementor мусор); arbitrary values max-h-[320px] (могут не сгенериться) вместо стандартных max-h-80 |
| D-172 | **Бэкап live от 9 июля ЕСТЬ на ПК; SSH/FTP на maksplit.ru закрыты (T-102).** maksplit.ru = vm-006d10f9.na4u.ru (хостинг na4u.ru/NetAngels). SSH port 22 = Connection timed out, FTP port 21 = closed, port 2022 = closed. e1.aurorix.net:2022 (89.35.130.4, другой IP) отвечает но это ДРУГОЙ сервер. rsync деплой через SSH НЕВОЗМОЖЕН. БЭКАП С LIVE на ПК (от 2026-07-09): maksplit_db.sql (14.7MB, 96 таблиц, wps_ prefix) + wp-content/themes/eteon (старая тема) + plugins + uploads. Владелица: «сохранить на пк первоначальную версию с сервера» — бэкап УЖЕ есть (17 дней). Свежий скачать не могу (нет доступа). Альтернативы деплоя: (1) WP admin → Appearance → Themes → Add New → Upload zip темы; (2) панель na4u.ru (файловый менеджер); (3) владелица через панель скачивает свежий бэкап. УРОК: проверять SSH/FTP доступ ДО деплоя. Если закрыты — деплой через WP admin zip ИЛИ панель хостинга. Бэкап с live мог быть сделан ранее — проверять наличие перед новым | Предполагать SSH работает (DEPLOY Шаг 10 rsync сломан); пытаться rsync без проверки доступа |
| D-173 | **Форма заявки: CF7 + email (сейчас localStorage fallback) (T-102).** lead-form.php: CF7-aware — если $cf7_lead_id задан в Customizer + shortcode contact-form-7 существует → do_shortcode CF7, ИНАЧЕ HTML fallback с localStorage JS (заявки НИКУДА не отправляются, хранятся в браузере). Сейчас CF7 НЕ установлен → fallback. Владелица: «при отправки заявки на какую почту будет приходить? и через какой плагин?» ОТВЕТ: сейчас НИКУДА (localStorage). Для почты: установить Contact Form 7 (бесплатный) → создать форму → указать email получателя (в настройках CF7 Mail tab) → вставить ID формы в Customizer (StroyMaks 2026 → CF7 Формы). УРОК: HTML fallback форма = заявки теряются (localStorage). Для production ОБЯЗАТЕЛЕН CF7 (или WPForms) + email получателя. Проверять shortcode_exists('contact-form-7') перед do_shortcode | Оставлять HTML fallback для production (заявки теряются); хардкодить email в PHP (CF7 настраивается в админке) |
| D-174 | **Миниатюра главного фото в галерее (T-103).** product-image.php: было — главное фото (main_image_id) большое (#main-photo), миниатюры (`.gallery-thumb`) только из gallery_image_ids. Если gallery пустой — миниатюр НЕТ. Владелица: «хочу чтобы миниатюра главного фото тоже была». ФИКС wp-coder: $all_thumb_ids = array_merge([$main_image_id], $gallery_image_ids) + array_unique — главное фото ПЕРВОЙ миниатюрой (active по умолчанию, border-color primary, opacity 100, aria-current true). Условие показа → if (!empty($all_thumb_ids)) вместо if (!empty($gallery_image_ids)). Верификация Playwright: domino 2 миниатюры (главное active=true 64×64 + gallery active=false 64×64); osen (gallery=0) 1 миниатюра (главное). УРОК: WooCommerce стандарт = главное фото + gallery как thumbnails. Наш override пропускал главное. Главное фото первым + array_unique (на случай дубликата в gallery) | Показывать thumbnails только из gallery (главное фото без миниатюры если gallery пуст); дублировать если main_image_id в gallery (без array_unique) |
| D-175 | **CF7 форма 4724 «Заявка СтройМакс» → d88a@yandex.ru (T-103).** CF7 (contact-form-7 6.0.5) был установлен но НЕ активирован → активирован wp-cli. Первая попытка: создал форму 6600 через wp_insert_post (post_type=wpcf7_contact_form, post_content=шорткоды) → форма ПУСТАЯ (только `<form>` без полей). КОРЕНЬ: CF7 6.x хранит форму в **_form** post_meta (не post_content!). 6600 имела только _mail + _locale, без _form → пустой рендер. ФИКС: обновил СУЩЕСТВУЮЩУЮ 4724 «Contact form 1» (из бэкапа 2020, имеет _form/_hash/_messages/_mail_2) → _form=имя+телефон+сообщение (text* your-name, tel* your-phone, textarea your-message, submit «Отправить заявку»), _mail recipient=d88a@yandex.ru + subject «Заявка с сайта СтройМакс — [your-name]» + sender «СтройМакс <wordpress@maksplit.ru>» + body (Имя/Телефон/Сообщение), _messages русские, post_title→«Заявка СтройМакс». 6600 удалена. theme_mod stroymaks2026_cf7_lead_id=4724. lead-form.php: if ($cf7_lead_id && shortcode_exists('contact-form-7')) → do_shortcode('[contact-form-7 id="4724"]'). Верификация curl: wpcf7-f4724, «Ваше имя» 3, your-phone 2, «Отправить заявку» 1, input text 1, input tel 1, textarea 1, submit 1. Playwright: input=9, textarea=1, submit=1. d88a@yandex.ru НЕ виден в HTML (защита от спамеров). УРОК: CF7 6.x = _form post_meta (не post_content). Создание формы через wp_insert_post БЕЗ _form = пустая форма. Лучше обновлять существующую форму (из бэкапа) ИЛИ добавить _form meta при создании. Поля: имя+телефон+сообщение (БЕЗ email — владелица не просила) | Создавать CF7 форму через wp_insert_post без _form meta (пустой рендер); хардкодить форму в post_content (CF7 6.x игнорирует post_content для формы) |
| D-176 | **SMTP для почты: PHP mail() на live ИЛИ WP Mail SMTP + Yandex (T-103).** CF7 отправляет через wp_mail() → PHP mail(). Локально Docker НЕТ MTA (sendmail) → тест отправки невозможен. На live NetAngels: PHP mail() обычно работает (Sendmail), но часто попадает в спам ИЛИ блокируется. Надёжнее: WP Mail SMTP + Yandex SMTP (smtp.yandex.ru:465 SSL, login=d88a@yandex.ru, пароль=пароль от ящика ИЛИ app password если 2FA включена). Пароль от d88a@yandex.ru у архитектора НЕТ → спросить владелицу. УРОК: для доставки почты на Yandex → Yandex SMTP (свой сервер). PHP mail() с wordpress@maksplit.ru часто в спаме. Если 2FA на d88a@yandex.ru → app password (не основной). На live: сначала попробовать PHP mail() (CF7 default), если не доходит → WP Mail SMTP + Yandex SMTP | Полагаться на PHP mail() без проверки доставки; использовать основной пароль ящика при 2FA (нужен app password) |
| D-177 | **Деплой на live: данных сервера НЕТ, SSH/FTP закрыты (T-103).** maksplit.ru = vm-006d10f9.na4u.ru (NetAngels/na4u.ru). SSH 22/2022 = timeout, FTP 21 = closed. rsync НЕВОЗМОЖЕН. na4u.ru = бренд хостинга NetAngels (российский провайдер) — владелица подтвердила «у нас нетангелс - провайдер». У архитектора НЕТ: логина/пароля панели NetAngels, FTP/SSH данных, WP admin доступа maksplit.ru. Бэкап с live на ПК от 2026-07-09 (maksplit_db.sql 14.7MB + eteon + plugins + uploads). Свежий бэкап скачать НЕ могу (нет доступа). Варианты деплоя: (1) WP admin maksplit.ru → Appearance → Themes → Upload zip темы (нужен WP admin доступ); (2) панель NetAngels/na4u.ru → файловый менеджер (нужен логин/пароль панели); (3) БД → phpMyAdmin в панели (для переноса CF7 формы + theme_mod + контент). УРОК: проверять доступ ДО деплоя. na4u.ru = NetAngels бренд (не отдельный провайдер). Для деплоя темы+БД нужен доступ к панели ИЛИ WP admin. Спросить владелицу: логин/пароль панели NetAngels (https://cp.netangels.ru) ИЛИ FTP/SSH ИЛИ WP admin maksplit.ru | Предполагать SSH работает (закрыт); путать na4u.ru с отдельным провайдером (это бренд NetAngels); деплоить без бэкапа live |
| D-178 | **🔥 SSH РАБОТАЕТ через eis-vds! maksplit + school на ОДНОМ сервере (T-104).** Владелица: «поищи в проекте school» → найден `D:/Anna/Сайты/school/DEPLOY_AND_SERVER.md` с SSH config `Host eis-vds HostName SERVER_IP User root IdentityFile ~/.ssh/id_ed25519_eisparser`. Проверка: school HostName SERVER_IP = IP maksplit.ru (D-172)! SSH-ключ `~/.ssh/id_ed25519_eisparser` ЕСТЬ на ПК. `ssh eis-vds` → SSH_OK, hostname=vm-006d10f9, root, uptime 124 days. **D-172/D-177 «SSH закрыт» — НЕВЕРНО**, SSH работает через eis-vds (порт 22, ключ). maksplit + school + fhtrg.ru + mcaist.ru + trakradar.ru + eisparser + bybit-ws — все на одном VDS NetAngels. УРОК: проверять SSH config других проектов на ПК (~/.ssh/config) — может быть доступ к нужному серверу. IP-адрес = ключ к поиску общего сервера | Считать SSH закрытым без проверки ~/.ssh/config других проектов; просить у владелицы доступ когда ключ УЖЕ есть на ПК |
| D-179 | **maksplit = нативный LEMP (НЕ Docker), WordPress 6.9.1 + WC 9.7.2 STABLE (T-104).** Разведка сервера через ssh eis-vds. Путь: /var/www/maksplit.ru/www (владелец eisparser:eisparser). nginx /etc/nginx/sites-available/maksplit.ru.conf: root /var/www/maksplit.ru/www, php8.2-fpm unix socket /run/php/php8.2-fpm.sock, SSL Let's Encrypt (истекает 2026-09-26, 60 дней, авто certbot.timer), redirect 80→443. PHP 8.2 + модули curl/gd/mysqli/mbstring/xml/zip/intl/SimpleXML. MariaDB 10.11.14 localhost:3306. БД: maksplit_db / maksplit_user / wps_ prefix / WP_DEBUG=false. Пароль в wp-config.php (извлечение: `PASS=$(awk -F\' '/DB_PASSWORD/{print $4}' wp-config.php)`). WordPress 6.9.1, **WooCommerce 9.7.2 STABLE** (НЕ 11.0.0-beta как в Docker! → на live производительность БУДЕТ нормальная, не 17-22s). Активная тема: eteon (СТАРАЯ). Активные плагины (10): redux-framework, cleantalk-spam-protect, contact-form-7, elementor-theme-core, elementor, revslider, woocommerce, wp-seopress-pro, wp-seopress, yml-for-yandex-market. blogname «Производитель тротуарной плитки. Челябинская область.». siteurl/home = https://maksplit.ru (уже HTTPS, менять НЕ нужно). uploads 150MB. Диск: 25G, 17G used, 7.2G avail. УРОК: maksplit НЕ Docker (в отличие от school). WC 9.7.2 stable на live = производительность OK. Старые плагины (elementor/revslider/redux/cleantalk/seopress-pro) — мусор, деактивировать при деплое. siteurl уже HTTPS | Считать maksplit в Docker (school в Docker, maksplit нет);担心 производительность на live (WC stable, не beta); деплоить elementor/revslider на live (старый мусор) |
| D-180 | **Бэкап live СКАЧАН + DEPLOY_AND_SERVER.md создан (T-104).** Владелица: «бэкап скачивай» + «создай в проекте такой же файл [как school DEPLOY_AND_SERVER.md]». (1) БЭКАП LIVE (свежий, 2026-07-27): mysqldump maksplit_db → /tmp/maksplit_db_live.sql (15MB) → scp на ПК `D:/Anna/Сайты/maksplit.ru/backups/maksplit_db_live_20260727.sql`; tar wp-content (themes+plugins+uploads) → /tmp/maksplit_wpcontent.tgz (217MB) → scp `D:/Anna/Сайты/maksplit.ru/backups/maksplit_wpcontent_20260727.tgz`. Пароль БД извлечён через awk (mysqldump через `PASS=$(awk...)`). (2) ФАЙЛ: делегирован coder → `D:/Anna/Сайты/maksplit.ru/DEPLOY_AND_SERVER.md` (312 строк, 16 секций) по образцу school: SSH config, URL, пути, LEMP сервисы, БД, текущее состояние, nginx conf, SSL, бэкапы, деплой stroymaks2026 (6 шагов: build→backup→upload→activate→plugins→cache), импорт БД из Docker (⚠️ перезапишет live), проверка, мониторинг, отличия от school, история. Пароль БД НЕ в файле (только команда awk). УРОК: бэкап live = mysqldump + tar wp-content, скачать через scp. DEPLOY_AND_SERVER.md = SSOT для деплоя (по образцу school). Теперь деплой stroymaks2026 возможен: scp темы → активация через mysql → деактивация старых плагинов | Делать деплой без свежего бэкапа live; хардкодить пароль БД в DEPLOY_AND_SERVER.md (только команда awk) |

| D-181 | **Дашборд niche-aware: 7-я ось + максплит-хардкоды удалены (T-105).** Жалоба владелицы: дашборд Make UI (http://localhost:8765/dashboard.html#editorial-cream) выдаёт максплит-подобный дизайн даже при смене темы/шрифтов/скруглений — layout/композиция остаётся как у СтройМакс v7. КОРЕНЬ: дашборд D-133 (Theme & Color Dashboard, 6 осей) был создан ПОД максплит — live preview (правая колонка) захардкожен под строительный каталог: «Смотреть каталог →», «Бетон М400 (B30) / 4 850 ₽/м³», «В наличии», picsum seed/build. При смене темы через var(--color-*) менялись ТОЛЬКО цвета/шрифты, контент оставался максплитовский → владелица видела «всё равно как максплит7». ПЛЮС layout pattern #9 useCase='Construction/Architecture (СтройМакс!)' (хардкод), mood industrial description='Утилитарный, бетон', theme concrete-steel description='уверенность строящегося здания'. ФИКС (делегирован coder, 13 правок в dashboard.html 1561→1792 строк): (A) Niche-переключатель = 7-я ось — NICHE_PRESETS объект 5 ниш (portfolio/ecommerce/media/corporate/saas) + Section 0 HTML перед Theme Grid + activeNiche state (default='portfolio' НЕ ecommerce) + renderNicheGrid/setNiche/applyNicheToPreview (меняет ТОЛЬКО preview контент: btn/card img-title-desc-price-unit/badge/link, НЕ CSS-токены) + URL hash новый формат #theme=X&niche=Y (backward compat: #editorial-cream → theme=editorial-cream,niche=portfolio) + JSON export niche/nicheName поля; (B) layout #9 useCase → 'Портфолио, архитектура, строительство' (без «СтройМакс!»); (C) layout wireframe → section labels (getWireframeLabels: каждая схема = массив подписей секций под wireframe); (D) mood industrial description → 'Утилитарный, индустриальный'; (E) theme concrete-steel description → 'Утилитарный, индустриальный stone' (без «строящегося здания'). Верификация: node -e JS-синтаксис OK (3 script blocks, 0 ошибок), grep «СтройМакс!» = 0, скриншот Playwright (#theme=editorial-cream&niche=portfolio) подтверждает — кнопка «Смотреть работы →» (НЕ «Смотреть каталог»), карточка «Eisparser / Next.js платформа аналитики» (НЕ «Бетон М400»), бейдж «Featured» (НЕ «В наличии»), niche-переключатель слева с 5 опциями (активна Портфолио), editorial-cream тема (cream #FBF9F4 + mocha + Playfair Display). УРОК: дашборд «Theme & Color» меняет только CSS-токены (цвета/шрифты/скругления) — для разнообразия генерации нужна отдельная ось Niche которая меняет КОНТЕНТ preview. Layout axis меняет только SVG-скелет — нужны section labels чтобы видеть реальную структуру. 6 осей = token-level diversity, 7-я ось (niche) = content-level diversity. Максплит-хардкоды в системных файлах = антипаттерн (дашборд должен быть niche-agnostic) | Оставить 6 осей без niche (продолжать подсовывать максплит-контент для портфолио); хардкодить контент preview под один проект |

| D-182 | **Дашборд: real-time цвет + 16 тем (T-106).** Две жалобы владелицы: (1) «когда меняешь цвет вручную, не меняется на превьешке» — customColor (color picker) и customHexInput (text input) event listeners 'input' только синхронизировали UI между собой, но НЕ вызывали applyAccent → превью не менялось в реальном времени при вводе/выборе цвета (применялось только после Enter или клика OK). Фикс: в оба input listener добавить setCustomHex(val) (customColor — всегда валидный #rrggbb, customHexInput — только когда полный 7-символьный hex набран). setCustomHex вызывает applyAccent + updateAccentSwatches. Присвоение .value НЕ триггерит 'input' → нет цикла. (2) «всего так мало тем? 7 штук... по идеи 7 цветов, изменить можно только акцент» — в файловой системе themes/ было 16 .md тем, а в THEMES объекте дашборда только 7 (concrete-steel, dusty-slate, graphite-mono, sage-stone, editorial-cream, modern-clean, warm-minimal). Не хватало 9: aurora, bento, bold-tech, dark-tech, earth-stone, gaming, industrial-dark, luxury, mesh-gradient. Делегирован coder: прочитать каждый .md, извлечь CSS-токены (16 переменных: --color-bg-page/bg-alt/surface/text-primary/text-muted/primary/primary-hover/primary-subtle/border/border-hover + --shadow-brand/card/card-hover + --transition-smooth + --font-display/body/mono), восполнять недостающие из описания (dark-tech/gaming/luxury .md имели только 6 базовых цветов — восполнил hover/subtle/border-hover/shadows из mood). Итог: THEMES = 16 объектов (7 light + 9 новых: 5 dark — aurora/bold-tech/dark-tech/gaming/industrial-dark, 4 light — bento/earth-stone/luxury/mesh-gradient). Верификация: node -e 0 ошибок, grep -c тем = 16, скриншот bold-tech (dark neutral-950 + cyan) + niche=portfolio показывает 16 тем в панели + Eisparser/Смотреть работы/Featured. УРОК: event listener 'input' должен применять изменения в реальном времени, не только синхронизировать UI — иначе пользователь не видит отклика. Темы в дашборде должны соответствовать themes/ папке (SSOT) — если .md есть, он должен быть в дашборде. Dark темы требуют --color-text-primary светлым + --color-surface на 1-2 тона светлее bg-page | Оставить input listener без applyAccent (пользователь не видит real-time отклик); добавлять темы в дашборд вручную по одной (SSOT нарушен — .md есть, в дашборде нет) |

| D-183 | **Дашборд: 8-я ось «Фон» (Background) + auto-luminance (T-107).** Владелица: «нужно тогда, чтобы была и возможность смены фона». До этого фон = --color-bg-page тема-зависимый (привязан к теме). Фикс: 8-я ось Section 3.5 «Фон страницы» между Custom Hex и Mood. 8 пресетов (4 light: White/Cream/Slate-50/Stone-100 + 4 dark: Slate-900/Neutral-950/Zinc-900/Pure Black) + custom color picker + hex input + Reset кнопка. Логика applyBackground(hex, scheme): перебивает --color-bg-page + авто text-primary/text-muted/border/surface/bg-alt (dark scheme → #F8FAFC/#94A3B8/#1E293B/lightenHex; light scheme → #0F172A/#475569/#E2E8F0/#FFFFFF/darkenHex). Accent НЕ трогается (остаётся от темы/accent). Хелперы: lightenHex (mix toward white), luminance (0.299r+0.587g+0.114b), isDarkBg (<0.5). setCustomBackground авто-detect scheme через isDarkBg. setTheme сбрасывает bg override (новая тема = новый дефолт). URL hash: &bg=HEX (персистится, без #). JSON export: background/backgroundCustom/backgroundScheme. Верификация Playwright (D-053 НЕ доверяем vision): 8 bg-swatch в DOM, клик Neutral-950 → bg #0A0A0F + text #F8FAFC (АВТО) + accent #6B5D4F (НЕ тронут) + URL &bg=0A0A0F; Reset → bg #FBF9F4 (тема) + text #2A2520 + &bg исчез; URL &bg=000000 → bg #000000 + авто dark; page errors none. Скриншот dark editorial (editorial-cream serif mocha на neutral-950) = премиум тёмная serif атмосфера. УРОК: фон должен быть отдельной осью от темы — даёт combos типа «dark editorial» (light тема на dark bg) / «светлый tech» (dark тема на light bg). Auto-luminance решает контраст (dark bg → auto светлый текст), accent остаётся от темы. 8 осей × 16 тем × 5 ниш × 9 layout × 7 mood × 4 radius × 5 font × 8 bg = огромное разнообразие генерации | Привязывать фон к теме (нельзя сделать dark editorial); менять ТОЛЬКО bg без авто text (нечитаемый контраст); трогать accent при смене фона (accent от темы) |

| D-184 | **Дашборд: шрифты independent selection + Recommendation notice (T-108).** Две жалобы владелицы: (1) «шрифты, хочу возможность подбирать шрифты» — FONT_PAIRS было 5 preset пар, нельзя выбрать display и body независимо, нет custom font; (2) «базовый дашборд = рекомендация, архитектор может предложить свои цвета/шрифты в диалоге» — шапка не упоминала recommendation. Фикс (делегирован coder, 11 правок A-K): (A) Recommendation notice в header — иконка ℹ + текст «Рекомендация — архитектор может предложить свои варианты в диалоге» (заменил «Pi Make» span); (B) Google Fonts <link> расширен с 6 до 18 шрифтов (+Manrope, Geist, Fraunces, Bricolage Grotesque, Plus Jakarta Sans, Syne, Outfit, Archivo, IBM Plex Sans, Crimson Pro, Cormorant, DM Sans); (C) FONT_PAIRS расширен с 5 до 17 пар (+12: manrope/manrope, geist/geist, fraunces/inter, bricolage/inter, plus-jakarta/inter, syne/inter, outfit/inter, archivo/inter, ibmplex/ibmplex, crimson/sourceserif, cormorant/inter, dm/dm); (D) AVAILABLE_FONTS массив (18 шрифтов с css-стеками, category sans/serif/mono/display); (E) HTML 2 <select> (font-display-select, font-body-select) + label «Подобрать независимо» после font-grid; (F) state activeDisplayFont/activeBodyFont + DOM refs; (G) функции renderFontSelects/updateFontSelects/setDisplayFont/setBodyFont + setFontPair/setTheme сбрасывают independent; (H) event listeners change; (I) init renderFontSelects; (J) URL hash &df=/&bf= персист; (K) JSON export font_pair.independent:true. Верификация Playwright (D-053): font-pair-card=17 (было 5), font-display-select options=19 (1 авто + 18), Recommendation в header=True, Google Fonts link расширен (grep 18 family=), independent selection — select Manrope → --font-display:'Manrope',sans-serif + URL &df=Manrope, select Geist body → --font-body:'Geist',sans-serif, reset → Playfair (тема), page errors none. УРОК: шрифты должны быть independent (display ≠ body) — preset пары = quick start, селекты = тонкая настройка. Google Fonts <link> должен включать ВСЕ шрифты из AVAILABLE_FONTS иначе fallback на системный. Дашборд = recommendation (не диктат), архитектор предлагает альтернативы в диалоге | Оставить 5 preset пар (нет independent выбора); подключать шрифты без Google Fonts link (fallback); считать дашборд диктатором а не рекомендацией |
| D-185 | **Дашборд: 9-я ось «Вариант карточки» (Card Variant) niche-зависимая (T-108).** Владелица: «все карточки одинаковые, например, товаров или услуг или новостей, исходя из сайта. есть же множество вариантов карточек, а у нас только один вариант.» Корень: preview-card = ОДИН вариант (фото + название + описание + цена), composition-primitives.md имеет Feature variants (Icon Grid/Alternating/Bento/Stat) но НЕ niche-зависимых card variants (product/project/article/service). Фикс (делегирован coder через ТЗ-файлы D:/pi/tmp_card_variants_tz.md + D:/pi/tmp_card_preview_fn.js из-за ENAMETOOLONG spawn, 9 правок A-I): (A) CARD_VARIANTS объект — 5 ниш × 4 варианта = 20: portfolio [Minimal/Thumbnail/Hover Overlay/Large Feature], ecommerce [Compact/With Badge/Quick View/Detailed], media [Vertical/Horizontal/Large Lead/Text Only], corporate [Icon+Text/Numbered/With Photo/Minimal], saas [Bento/Icon Grid/Stat/Alternating]; (B) CSS .card-variant-card; (C) HTML Section 7.5 между Layout и Export; (D) state activeCardVariant + DOM cardVariantGrid; (E) функции renderCardVariantGrid/updateCardVariantCards/setCardVariant + renderCardPreview (20 case — каждая variация = уникальная HTML структура с CSS-токенами var(--color-*/--font-*/--radius-card), использует NICHE_PRESETS[activeNiche].preview данные); (F) setNiche сбрасывает variant на первый + re-render + **УБРАН блок if(card) из applyNicheToPreview** (card content теперь управляется renderCardPreview, applyNicheToPreview только btn/badge/link/label); (G) init renderCardVariantGrid + cv из URL; (H) URL &cv= персист; (I) JSON export card_variant/card_variant_name. Верификация Playwright (D-053): portfolio 4 variant [Minimal/Thumbnail/Hover Overlay/Large Feature] active=Minimal, pf-minimal img=0 + PROJECT label, click pf-thumbnail → img=1 + URL &cv=pf-thumbnail, click pf-large-feature → flex layout + FEATURED + img=1, switch ecommerce → [Compact/With Badge/Quick View/Detailed] active=Compact + card ≠ Eisparser, click ec-with-badge → badge=1 + price 4 850, switch media → [Vertical/Horizontal/Large Lead/Text Only], switch saas → [Bento/Icon Grid/Stat/Alternating], page errors none. Скриншот bold-tech dark + portfolio + pf-large-feature = asymmetric (фото 50% + FEATURED лейбл + текст 50%) cyan акцент — существенно отличается от стандартной карточки. УРОК: card variant = niche-зависимая ось (не универсальная) — product cards ≠ project cards ≠ article cards. renderCardPreview полностью заменяет innerHTML preview-card, поэтому applyNicheToPreview больше НЕ трогает card content (разделяй ответственность: niche → btn/badge/link/label, card variant → card structure). 20 case = 5 ниш × 4 уникальные структуры (не косметика, а РАЗНЫЕ layout: minimal без фото / thumbnail фото сверху / horizontal фото слева / large-feature asymmetric / text-only без фото / stat крупные числа / numbered большие номера / bento разноразмерные плитки). ТЗ-файлы (вместо inline) решают ENAMETOOLONG для длинных ТЗ | Оставить 1 вариант карточки (владелице мало разнообразия); делать card variant универсальной (не niche-зависимой — product ≠ project); оставлять applyNicheToPreview трогать card content (конфликт с renderCardPreview) |

| D-186 | **Деплой stroymaks2026 на live maksplit.ru ЗАВЕРШЁН (T-109).** Владелица: «максплит переноси». Стратегия: деплой ТОЛЬКО темы (НЕ импорт Docker БД — она старая eteon, перезапишет товары/заказы) + точечные правки live БД. Backup live (свежий 2026-07-28 08:33): /root/maksplit_live_backup_20260728_083321.sql (15M) + eteon.bak_20260728_083321 + active_plugins_backup_deploy в БД. Этапы: (1) code-auditor анализ: все 4 page-*.php = Custom Page Templates (Template Name header, не template hierarchy), front-page.php не зависит от slug — нужна правка 16 hardcoded URL. (2) wp-coder: 10 правок в functions.php (6 строк fallback menu: /about/→/company-about/, /delivery/→/dostavka/, /contacts/→/contact-us/) + category-grid.php (4 строки /catalog→/shop), /calculator НЕ тронут (страница создана через SQL). php -l: No syntax errors. (3) npm run build (tailwind.css 30K). (4) tar --force-local (Windows bsdtar интерпретирует D: как remote host, --force-local решает) — 3.0M, 46 файлов, 0 node_modules. (5) scp /d/pi/ (POSIX путь, не D:/ — scp тоже интерпретирует : как remote). (6) Распаковка + chown eisparser:eisparser 644. php -l на сервере: OK. (7) SQL через файл (heredoc ломает кавычки в serialized array): активация темы (stylesheet/template=stroymaks2026) + 3× _wp_page_template (company-about→page-about, contact-us→page-contacts, dostavka→page-delivery) + CREATE page calculator (ID=6616, title «Калькулятор» через UNHEX D09AD0B0D0BBD18CD0BAD183D0BBD18FD182D0BED180, D-147) + _wp_page_template=page-calculator.php + deactivate 6 плагинов (оставить CF7+WooCommerce+SEOPress+YML). (8) blogname «Производитель тротуарной плитки. Челябинская область.» → «СтройМакс» через UNHEX D0A1D182D180D0BED0B9D09CD0B0D0BAD181. (9) nginx + php8.2-fpm reload. Результат: 7/7 страниц HTTP 200 (home/company-about/contact-us/dostavka/calculator/shop/wp-admin 302), body class=stroymaks2026, h1 новые (Брусчатка которая служит десятилетиями / Калькулятор / Каталог продукции / Контакты / Доставка), product/domino h1+price вариации 850-1100₽, CF7 рендерится (ID 4724), SEO meta присутствует, 0 JS errors, 0 PHP fatal. УРОК: (1) Live slug'и ≠ Docker slug'и ≠ тема hardcoded URL — нужен аудит до деплоя (code-auditor). (2) Custom Page Templates назначаются через _wp_page_template meta, не template hierarchy — slug mismatch НЕ ломает отображение, но ломает hardcoded home_url() ссылки. (3) Windows tar/scp интерпретируют D: как remote host — --force-local для tar, POSIX /d/ для scp. (4) SQL с serialized array (active_plugins) и Cyrillic — записывать в файл, не heredoc (кавычки + кодировка). (5) UNHEX для Cyrillic в INSERT/UPDATE (D-147 подтверждён для live MariaDB тоже). (6) Не импортировать Docker БД на live — Docker = старый dump, live = production с заказами. Альтернативы отклонены: переименовать slug'и в БД (сломает SEO, нужны 301), переименовать файлы темы (дублирование Custom Template логики), импорт Docker БД (перезапишет товары/заказы) | Оставить hardcoded URL (404 в fallback menu), переименовать slug'и (301 + потеря SEO), импорт Docker БД (потеря заказов) |

| D-187 | **4 пост-деплой правки maksplit.ru live (T-110).** Владелица: 4 жалобы после деплоя (T-109). (1) «Производим ... в Юрюзани» → «в Челябинской области» в hero subtitle. Корень: hero.php:23 имеет СВОЙ СОБСТВЕННЫЙ inline default в get_theme_mod() — дублирует functions.php:485 default (нарушение SSOT P1). Первая правка functions.php НЕ сработала — hero.php inline default переопределяет. Фикс: правка hero.php:23 (второе место). УРОК: get_theme_mod() default в шаблоне переопределяет Customizer register default — нужно менять ВЕЗДЕ либо убрать inline default и полагаться только на register. (2) Фильтр каталога НЕ работал. Корень: archive-product.php рендерит .filter-tab + data-filter, content-product.php имеет data-category, но main.js искал .filter-btn + data-category + is-active + aria-pressed — СЕЛЕКТОРЫ НЕ СОВПАДАЛИ (3 расхождения: класс кнопки, класс active, aria-атрибут). Фикс wp-coder: main.js .filter-btn→.filter-tab, data-category→data-filter (на кнопках), is-active→active, aria-pressed→aria-selected. content-product.php УЖЕ имел data-category (правка не нужна). Верификация Playwright: 5 tabs (Все/Бордюры, водостоки/Брусчатка тротуарная/Памятники из бетона/Тротуарная плитка), 16 карточек с data-category, клик «Бордюры, водостоки» → 2 видимых (16→2), фильтр РАБОТАЕТ. УРОК: фильтр = тройная связка archive-product.php(.filter-tab+data-filter) × content-product.php(data-category) × main.js(селекторы) — все три файла должны быть синхронизированы. aria-pressed для toggle, aria-selected для tabs. (3) Калькулятор «₽/м²» переносилось на новую строку. Фикс: whitespace-nowrap на 3 статичных span (строки 199/209/219) + 3 textContent→innerHTML с <span class=whitespace-nowrap> (строки 660/684) + родительский контейнер. Верификация: 9 whitespace-nowrap, 3 span с «₽/м²». (4) Фавикон отсутствовал (wp_head есть, site_icon не загружен). Фикс: functions.php wp_head hook приоритет 20 — если get_option('site_icon') пустой, выводит SVG inline base64 с буквой «С» (СтройМакс) Georgia serif на фоне Dusty Blue #32598f с rounded corners 14px + apple-touch-icon + mask-icon. add_theme_support('site-icon') добавлен. Верификация: link rel=icon href=data:image/svg+xml;base64,... присутствует в HTML. УРОК: SVG inline favicon через base64 — не требует файла uploads, работает сразу. Если владелица загрузит site_icon через Customizer — hook отключится (get_option проверка). Деплой: scp 4 файлов (functions.php, page-calculator.php, assets/js/main.js, template-parts/hero.php), php -l OK на live, postfix queue очищена (20 deferred писем на d88a@yandex.ru удалены postsuper -d ALL). | Оставить inline default в hero.php (дублирование, правка functions.php не сработает); переименовать data-filter в data-category (сломает архив); файл favicon (нужен uploads, сложнее) |
| D-188 | **SMTP на live maksplit.ru: порт 25 заблокирован NetAngels — нужен WP Mail SMTP + Yandex app password (T-110).** Владелица: «тестовый заказ из формы не пришёл». Диагностика: postfix установлен, PHP mail() возвращает true, но /var/log/mail.log показывает `connect to mx.yandex.ru[77.88.21.249]:25: Connection timed out` dsn=4.4.1 status=deferred — 20 писем в очереди на d88a@yandex.ru. Тест портов: 25 BLOCKED (timeout), 465 OK (openssl s_client connect успешно, SMTPS работает), 587 timeout, smtp.yandex.com:465 timeout. Причина: NetAngels VDS блокирует исходящий порт 25 (стандартная практика против спама), 465 (SMTPS) открыт. CF7 _mail meta: subject «Новый заказ с сайта», sender=www-data@vm-006d10f9.na4u.ru (некорректный From — yandex отклонит). РЕШЕНИЕ: WP Mail SMTP plugin (НЕ установлен на live) + Yandex app password (владелица создаёт на id.yandex.ru → Безопасность → Пароли приложений → Почта) + SMTP host=smtp.yandex.ru port=465 encryption=SSL user=d88a@yandex.ru. Очередь postfix очищена (postsuper -d ALL, 20 сообщений). Альтернатива: Brevo/Sendgrid API (интернет есть, curl к api.brevo.com OK) — но требует регистрации + API ключа. УРОК: VDS-провайдеры блокируют порт 25 против спама — WP mail() без SMTP relay не работает для внешних адресов. Проверять порты ДО деплоя формы. From: header должен быть доменным (www-data@... отклоняется). | Оставить postfix на порту 25 (письма永远 deferred); использовать PHP mail() без SMTP (не доходит); Brevo API (лишняя регистрация) |

| D-189 | **🔥 SMTP ОТМЕНЁН → postfix MX delivery РАБОТАЕТ (T-111).** D-188 был неверным диагнозом: проверка openssl s_client `| head` давала exit 0 обманчиво (от head, не openssl). Реальная проверка: openssl exit=124 (timeout), PHP fsockopen ssl://smtp.yandex.ru:465 FAILED 10s, bash /dev/tcp smtp.yandex.ru:465 BLOCKED. **NetAngels блокирует ВСЕ SMTP порты (25/465/587) ко ВСЕМ внешним серверам** (Gmail/SendGrid/Outlook/Yandex — все timeout), НЕ только 25. Но: (1) mx1.netangels.ru:25 OPEN, (2) mx0/mx1.maksplit.ru (A=MX_IP/8 = NetAngels серверы) :25 OPEN — провайдер открывает 25 к СВОИМ серверам. MX maksplit.ru = mx0.maksplit.ru (pri 1) + mx1.maksplit.ru (pri 5). SPF maksplit.ru = `v=spf1 a include:netangels.ru ~all` → наш VDS (A=SERVER_IP) МОЖЕТ отправлять от @maksplit.ru. Тест sendmail `-f admin@maksplit.ru me@maksplit.ru`: `status=sent (250 2.0.0 Ok: queued as 088C41FB51)` ✅. СТРАТЕГИЯ: CF7 recipient=me@maksplit.ru (владелица уже настроила в _mail meta) → postfix на VDS → MX mx0.maksplit.ru:25 → NetAngels принимает me@maksplit.ru → пересылка по панели NetAngels → реальный ящик. SMTP hook БЕСПОЛЕЗЕН (465 заблокирован) → УДАЛЁН. Но возникла 2-я проблема: PHP mail() ставит envelope sender (MAIL FROM) = user@vm-006d10f9.na4u.ru (www-data/root), а NetAngels требует envelope @maksplit.ru (SPF) → `451 4.7.1 Try again later` (deferred). wp_mail From header = admin@maksplit.ru, но envelope ≠ From. ФИКС: phpmailer_init hook `$phpmailer->Sender = $phpmailer->From` (envelope = From = admin@maksplit.ru) + wp_mail_from filter → admin@maksplit.ru. БЕЗ isSMTP/Host/Port/Password (postfix sendmail). wp-config.php SMTP константы + пароль REDACTED УДАЛЕНЫ (D-078). Финальный тест: `5023CA12: from=<admin@maksplit.ru>, to=<me@maksplit.ru>, relay=mx0.maksplit.ru[MX_IP]:25, dsn=2.0.0, status=sent (250 Ok)` ✅, очередь пуста. УРОК: (1) `| head` искажает exit code — проверять реальный exit без pipe. (2) NetAngels блокирует 25/465/587 ко внешним, НО открывает 25 к своим MX серверам — postfix + MX = рабочий путь без SMTP relay. (3) PHP mail() envelope sender = user@hostname, НЕ From header — нужно phpmailer_init Sender=From для SPF. (4) CF7 _mail recipient/sender хранятся в serialized array в _mail post_meta (CF7 6.x), НЕ в _form (там HTML шаблон) и НЕ в post_content (CF7 5.x). (5) Yandex app password НЕ ПОНАДОБИЛСЯ — владелица удалила/может удалить. Отправка от admin@maksplit.ru через SPF `a` (наш VDS). Альтернативы отклонены: WP Mail SMTP plugin (465 заблокирован, бесполезен), Brevo/SendGrid API (лишняя регистрация, 443 работает но overkill), Yandex app password (не нужен — postfix+MX достаточен) | Доверять `openssl | head` exit 0 (реально 124 timeout); использовать WP Mail SMTP (465 заблокирован); оставлять envelope sender user@hostname (451 4.7.1 SPF fail); хардкодить Yandex app password в wp-config (D-078, не понадобился) |

| D-190 | **🔥 CF7 форма НЕ рендерилась на live — theme_mod отсутствовал (T-112).** Владелица: «моих тестовых нет» (а мои wp_mail тесты дошли). Диагностика wp-coder: lead-form.php проверяет `get_theme_mod('stroymaks2026_cf7_lead_id','')` → на live БД `theme_mods_stroymaks2026` = `a:1:{s:18:"custom_css_post_id";i:-1;}` (НЕТ ключа cf7_lead_id). Условие `$cf7_lead_id && shortcode_exists('contact-form-7')` → FALSE → рендерился **HTML fallback с localStorage** (форма выглядит рабочей, но данные сохраняются в браузере, никуда не отправляются). Владелица заполняла fallback. Корень: при деплое T-109 (D-186) мы перенесли ФАЙЛЫ темы (scp) + target SQL правки, но `theme_mods_*` опция Customizer — serialized array в wps_options — НЕ перенеслась (на Docker D-175 мы её установили, на live — нет). ФИКС: `php -r 'require("wp-load.php");set_theme_mod("stroymaks2026_cf7_lead_id",4724);'` (надёжнее чем `wp option patch` для serialized). Верификация: curl https://maksplit.ru/ → `wpcf7-f4724-o1` присутствует (4 поля: name/email/phone/message). wp_mail тест: `status=sent (250 2.0.0 Ok: queued as D30491FB67)` (0.17s). Очередь очищена (115→0, старые d88a@yandex.ru deferred удалены). NetAngels greylisting (451 Try again later) на первом тесте — нормально, ретрай через 1-2 мин проходит. УРОК: (1) **theme_mods_* Customizer = serialized array в wps_options, НЕ переносится при scp темы** — нужен targeted SQL/PHP после деплоя. (2) Fallback форма с localStorage выглядит рабочей но НЕ отправляет — нужно верифицировать РЕНДЕР реальной CF7 (grep wpcf7-f<ID>), а не только наличие формы визуально. (3) `set_theme_mod()` через `php -r wp-load.php` надёжнее `wp option patch` для serialized массивов. (4) Добавить в wp-integration/SKILL.md Post-deploy чеклист: проверить все theme_mods Customizer (cf7_lead_id и др.). | Считать что форма работает если она видна визуально (fallback обманчив); `wp option patch` для serialized theme_mods (ломает массив); переносить тему без верификации theme_mods |

<!-- ID: D-001, D-002, ... — сквозная нумерация по проекту. -->
| D-191 | **🔥 Фильтр каталога НЕ работал на live — двойной класс product-card (T-113).** Владелица: «не работают фильтры в каталоге» (2-я жалоба — D-187 «чинили» но Playwright не поймал). Диагностика wp-coder: класс `product-card` использовался на ДВУХ уровнях DOM — `<li class="product-card" data-category="slug">` (обёртка, content-product.php) И `<article class="product-card">` (контент, template-parts/product-card.php). Инлайн JS в archive-product.php: `productGrid.querySelectorAll('.product-card')` находил ОБА элемента. При фильтрации: `<li>` имеет data-category → фильтруется правильно; `<article>` НЕ имеет data-category → `getAttribute('data-category')=null` → `null===slug` → false → `display:none` на article. Результат: `<li>` правильно скрывался/показывался, но `<article>` внутри ВСЕ получали display:none → карточки пустые при любом фильтре (кроме «Все» где JS сбрасывал только li). ФИКС: 1 строка в archive-product.php инлайн JS: `.product-card` → `[data-category]` (ищет только li с data-category, не трогает article). Локальный эталон + live оба исправлены. Верификация Playwright (объективная, D-053): 16 карточек (Все) → клик «Бордюры, водостоки» → 2 visible li [bordyury,bordyury] → клик «Все» → 16 → клик «Брусчатка тротуарная» → 7 [bruschatka×7]. Slugs data-filter (табы) = data-category (карточки): all/bordyury/bruschatka/plitka/памятники-из-бетона. main.js HTTP 200 ?ver=6.9.1. УРОК: (1) **Дублирующий класс на разных уровнях DOM = ловушка для querySelectorAll** — селектор по классу ловит ОБА, нужно уточнять через атрибут `[data-category]` или контейнер `li.product-card`. (2) **D-187 Playwright НЕ поймал баг** — проверял видимость `<li>` (display), а `<article>` внутри был скрыт. Playwright-верификация должна проверять РЕНДЕР КОНТЕНТА (article visible offsetParent, текст карточки), не только родителя. (3) Жалоба владелицы 2-й раз = grep/Playwright недостаточно — нужна проверка реального контента карточки после фильтра. | Использовать класс-селектор когда класс дублируется на родителе+ребёнке; верифицировать фильтр только по li display (без проверки article внутри) |


| D-192 | **МЁРТВАЯ ФОРМА order-form на калькуляторе = КОРНЕВАЯ ПРИЧИНА писем нет (T-114).** Владелица: всегда было написано что отправлено но письма не приходят, отправь сам с плейрайт (3-я жалоба, в гневе сколько будем по кругу ходить). Владелица прислала HTML формы — СЕКЦИЯ id=form-section заголовок Оформите заявку, форма id=order-form, кнопка Отправить заявку, div id=form-success Заявка отправлена. Это НЕ CF7! Это статическая HTML форма в page-calculator.php БЕЗ action, БЕЗ PHP-обработчика — JS просто показывал #form-success и сохранял в localStorage. Данные НИКУДА не уходили. ДВЕ разные формы на ДВУХ разных страницах: главная (/) → CF7 4724 (рабочая, я тестировал); калькулятор (/kalkulyator/) → order-form (МЁРТВАЯ, владелица заполняла). Архитектор 3 раза тестировал главную форму → она работала → почта работает. Но владелица заполняла калькулятор → письма не было. Корень: wp-coder на D-190 шаге 3 сказал Калькулятор НЕТ формы — ОШИБКА, он не нашёл order-form (она не wpcf7, grep по wpcf7 не нашёл). ФИКС wp-coder: (1) замена form id=order-form на do_shortcode contact-form-7 id=4724 через get_theme_mod calculator_id с fallback на lead_id 4724; (2) удалён 28-строчный JS localStorage-обработчик; (3) set_theme_mod calculator_id=4724; (4) секция-обёртка #form-section с заголовком сохранена; (5) php -l OK, scp деплой. Верификация Playwright на /kalkulyator/: заполнение → submit → status=sent, Ваш заказ отправлен, API mail_sent. Maillog: 12:39:57 from=admin@maksplit.ru to=me@maksplit.ru status=sent (250 2.0.0 Ok queued). УРОК: (1) Диагностика формы = проверять ВСЕ страницы с формами, не только главную — владелица может заполнять любую. (2) grep по wpcf7 НЕ найдёт мёртвые HTML формы — нужно grep по order-form/form-section/form-success/<form тоже. (3) отправлено без письма = localStorage fallback ИЛИ мёртвая HTML форма — всегда проверять maillog на реальную отправку. (4) Архитектор должен спрашивать владелицу КАКУЮ страницу и КАКАЯ форма (HTML) — не предполагать. (5) get_theme_mod fallback-цепочка (calculator_id → lead_id → 4724) = надёжность. | Тестировать только главную форму (владелица заполняла калькулятор); grep только wpcf7 (не найдёт order-form); оставить мёртвую HTML форму (письма потеряны навсегда) |

| D-193 | **Email поле CF7 4724 → необязательное (T-114).** Форма 4724 имела [email* your-email] (обязательное). Владелица заполняла имя+телефон БЕЗ email → CF7 валидация блокировала отправку (status=init, NO REQUEST к серверу, красная надпись Необходимо заполнить все поля под email). Playwright подтвердил: без email status=init, API NO_REQUEST. ФИКС: _form post_meta 4724 [email* your-email placeholder Email] → [email your-email placeholder Email] (убрать звёздочку). your-name* и your-phone* остались обязательными. Schema: email+maxlength rules БЕЗ required. Для формы Рассчитать стоимость email клиента не нужен — достаточно имени и телефона. УРОК: для lead-gen форм email часто лишний — имя+телефон достаточно. CF7 звёздочка * = required, убрал → optional. Schema кэшируется, но CF7 6.x регенерирует при update_post_meta. | Оставить email обязательным (валидация блокирует отправку без email); требовать email для lead-формы (барьер для клиента) |

| D-194 | **СИСТЕМНЫЙ УРОК: отправлено ≠ отправлено — 4 места где письма теряются (T-114 summary).** После 3 жалоб владелицы на почту, корневые причины в порядке обнаружения: (1) D-188→D-189: SMTP порт заблокирован → postfix+MX delivery (envelope Sender=From). (2) D-190: CF7 не рендерился (theme_mod missing) → localStorage fallback ВРАЛ работает. (3) D-193: email* валидация блокировала submit без email → отправлено НЕ появлялось (но владелица говорила отправлено — значит это была другая форма). (4) D-192: МЁРТВАЯ HTML форма order-form на калькуляторе — показывала Заявка отправлена через JS+localStorage, НИЧЕГО не отправляла. КОРНЕВАЯ. Архитектор проверял главную (работала) → почта работает × 3 — но владелица заполняла калькулятор. ПРОТОКОЛ диагностики почты: (a) спросить владелицу КАКАЯ страница + HTML формы; (b) проверить ВСЕ страницы с формами (grep wpcf7|order-form|form-section|<form|form-success|localStorage); (c) Playwright отправить с КАЖДОЙ страницы; (d) maillog: реальная строка to=... status=sent?; (e) НЕ доверять отправлено на экране — только maillog. Fallback формы (localStorage) = ДЕЦЕПТИВНЫ — выглядят рабочими, ничего не отправляют. УРОК: когда владелица говорит отправлено но письма нет — спрашивай HTML/скриншот формы, НЕ тестируй свою форму. | Доверять отправлено на экране; тестировать только главную форму; игнорировать жалобы владелицы как должна работать; grep только wpcf7 |


| D-195 | **CF7 форма 4724: оформление с labels + pre-fill из калькулятора (T-115).** Владелице нравилось оформление старой (мёртвой D-192) формы — labels над полями (Имя */Телефон */Email/Комментарий), placeholder'ы, кнопка «Отправить заявку» (не «Отправить»), текст согласия, И автозаполнение комментария данными из калькулятора. ФИКС на рабочей CF7 (НЕ возврат мёртвой): (1) _form CF7 4724 обновлён через update_post_meta — labels с for= (через id:cf7-name в тегах), placeholder'ы (Иван Петров/+7(999)123-45-67/ivan@example.com), кнопка «Отправить заявку» (class:smaks-submit), текст согласия (smaks-consent). your-name*(text*)+your-phone*(tel*) обязательны, your-email(email)+your-message(textarea) необязательны (D-193). (2) CSS labels в functions.php wp_head приоритет 30 — .smaks-form-grid flex column md+grid-2-cols, .smaks-field label text-sm font-medium var(--color-text-primary), .smaks-req color danger, .smaks-consent text-xs text-muted text-center, input.smaks-submit brand button. Существующие .wpcf7-form input[type=text] правила НЕ затронуты. (3) Pre-fill JS inline в page-calculator.php (IIFE, не конфликтует с calc JS) — buildCalcSummary() собирает сводку (категория/цвет/площадь/размер/кол-во/итог) из #calc-category/#calc-color-*/#calc-area/#calc-size-*/#calc-quantity/#calc-result → textarea[name=your-message].value. setTimeout(50) после change чтобы дождаться обновления #calc-result существующим скриптом. if(!calcCategory||!cf7Message)return — на главной молчит (нет calc элементов). Верификация Playwright: калькулятор — выбрать Брусчатка/Серый/50м² → your-message = «Заявка с калькулятора: • Категория: Брусчатка • Цвет: Серый • Площадь: 50 м² • Итого: 42 500 ₽» → submit → mail_sent. Главная — labels есть, pre-fill НЕ срабатывает, submit → mail_sent. Maillog: 13:50:30+13:50:38 status=sent to=me@maksplit.ru. УРОК: (1) CF7 6.x поддерживает HTML + id: в тегах — [text* your-name id:cf7-name] создаёт id → label for=cf7-name работает. (2) class:smaks-submit в [submit] добавляет класс к input, селектор .wpcf7-form input.smaks-submit. (3) Pre-fill JS с setTimeout(50) после change — надёжнее MutationObserver, ждёт обновление DOM другим скриптом. (4) НЕ нужно возвращать мёртвую форму для UX — CF7 + labels + pre-fill JS = тот же UX + рабочая отправка. (5) tel* для телефона — мобильная numeric клавиатура. | Возвращать мёртвую HTML форму (письма потеряны); оставлять только placeholders без labels (владелице не нравилось); pre-fill через MutationObserver (overkill, setTimeout проще) |

| D-196 | **Блог + 5 SEO-статей для Яндекса (T-116).** Владелица: «там были еще новости» + «напиши еще свои статьи» + «главное чтобы статьи помогали для поиска в яндексе». (1) Шаблоны блога: single.php (Article JSON-LD @type=Article для SEO Яндекса — headline/datePublished/author/publisher/mainEntityOfPage + semantic HTML article/header/time + entry-content CSS + related posts + breadcrumb), category.php (архив /category/novosti/, сетка карточек border+rounded-2xl+bg-surface с иконкой 📰, hover shadow-card), template-parts/latest-posts.php (WP_Query 3 свежих + кнопка «Все статьи»), front-page.php (секция после popular-products перед lead-CTA — товары→контент→лидогенерация), header.php (пункт «Статьи» → /category/novosti/ в fallback menu). УРОК: Tailwind CDN v3 НЕ резолвит CSS-переменные в arbitrary values (rounded-[var(--radius)] → 0px) — использовать rounded-2xl (=16px=var(--radius)). (2) 5 SEO-статей (coder): HTML 1800-2800 зн, реальные цены 850/1100/120/150/300 + контакты +7 982 341-69-70 + CTA на калькулятор, ключевые запросы в H1+первых абзацах (тротуарная плитка/брусчатка/укладка/бордюр/кавларобетон). ZERO INVENTION для товаров, но статьи-гиды = информационный контент (владелица разрешила). (3) wp_insert_post 5 статей ID 6645-6649, post_status=publish, рубрика novosti (term_id=1), даты размазаны 29.07-02.08 (естественно), SEOPress _seopress_titles_desc meta_description, post_excerpt. Кириллица через wp_insert_post (PHP), НЕ mysql cli (D-147). (4) 🔥 КРИТИЧНО: 6 старых постов (2024-2025) имели Cyrillic URL-encoded slugи (%d1%87%d1%82%d0%be...) — SEOPress sitemap декодировал в Cyrillic URL → nginx/WP 404 → Яндекс НЕ индексировал 6 статей. Фикс: wp_update_post Latin slugи (chto-takoe-kavlarobeton, gazonnaya-reshotka-dlya-ekoparkovki, kak-vybrat-trotuarnuyu-plitku, tekhnologiya-ukladki-plitki, novinka-plitka-romb-uzornyj, novinka-pamyatniki-iz-kavlarobetona). WP автоматически 301 со старых. pre_get_posts posts_per_page=-1 для is_category() (все посты на 1 странице). Sitemap: 13 URL чистый Latin, все HTTP 200. УРОК: Cyrillic slugи в WP = SEO-самоубийство; ВСЕГДА Latin slugи для постов; проверять sitemap на decoded Cyrillic. Article JSON-LD помогает Яндексе понять что это статья. | Оставить 5 старых постов с Cyrillic slugами (Яндекс 404, не индексируется); пагинация вместо posts_per_page=-1 (12 постов мало, 1 страница лучше для SEO); нет JSON-LD (Яндекс хуже понимает); выдумывать цены/характеристики (D-134 нарушение) |

| D-197 | **НОВОЕ УМЕНИЕ: Pi делает сайты в Elementor WordPress.** Востребованный навык — большинство заказчиков используют WP+Elementor, не кастомные темы. Skill `~/.pi/agent/skills/elementor-builder/` (SKILL.md + rules.md + 10 шаблонов + css/decorative.css). Формат: section+column (классический, работает в Elementor 3.x и 4.x, НЕ container/flex). Pipeline: HTML-прототип (Make UI) → конвертация в Elementor JSON → импорт через Templates → Site Settings (цвета/шрифты) → декоративный CSS в Custom CSS (Elementor Pro). Экспорт: `{"content": [...], "title": "...", "type": "section"}`. 10 готовых шаблонов: ai-saas, analytics, app, blog, conference, education, portfolio, pricing, studio, team (каждый: .json + .kit.json + .png + .html). Проверено на проекте «Барышня-крестьянка» (usadba): HTML-прототип одобрен, 6/9 блоков в Elementor JSON, формат выстрадан через 8 проблем (D-001..D-016 в usadba/DECISIONS.md). Ключевые инсайты: typography_* плоские ключи (НЕ вложенные), inline-стили работают в section+column (не container), `$$type` НЕ работает в 4.1.3, copy-paste между сайтами не работает (разные версии Elementor). Три pipeline теперь: (1) HTML+Tailwind (прототип/лендинг без CMS), (2) Кастомная WP-тема (полный контроль, Stroymaks2026), (3) Elementor JSON (заказчик сам правит через визуальный редактор). | Делать только кастомные темы (ограничивает рынок); не формализовать Elementor pipeline (потеря знаний при каждом новом проекте) |
